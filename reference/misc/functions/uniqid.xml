<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision$ -->
<!-- EN-Revision: 9a674654ec8fd683f1c69813fdff6672121addfc Maintainer: hirokawa Status: ready -->
<!-- CREDITS: takagi -->
<refentry xml:id="function.uniqid" xmlns="http://docbook.org/ns/docbook">
 <refnamediv>
  <refname>uniqid</refname>
  <refpurpose>一意な ID を生成する</refpurpose>
 </refnamediv>
 
 <refsect1 role="description">
  &reftitle.description;
  <methodsynopsis>
   <type>string</type><methodname>uniqid</methodname>
   <methodparam choice="opt"><type>string</type><parameter>prefix</parameter><initializer>""</initializer></methodparam>
   <methodparam choice="opt"><type>bool</type><parameter>more_entropy</parameter><initializer>false</initializer></methodparam>
  </methodsynopsis>
  <para>
   マイクロ秒単位の現在時刻にもとづいた、接頭辞つきの一意な ID を取得します。
  </para>
 </refsect1>

 <refsect1 role="parameters">
  &reftitle.parameters;
  <para>
   <variablelist>
    <varlistentry>
     <term><parameter>prefix</parameter></term>
     <listitem>
      <para>
       これが有用なのは、たとえば複数ホストで同時に ID
       を生成するような場合です。このような場合、同じマイクロ秒で同じ ID
       が生成されてしまう可能性があります。
      </para>
      <para>
       空の <parameter>prefix</parameter> を指定すると、
       返される文字列は 13 文字となります。
       <parameter>more_entropy</parameter> が &true; の場合は
       23 文字となります。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><parameter>more_entropy</parameter></term>
     <listitem>
      <para>
       &true; にすると、<function>uniqid</function> は
       返り値の最後にさらに別のエントロピーを
       (線形合同法を使用して) 追加します。これにより、結果がより一意になります。
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </para>
 </refsect1>

 <refsect1 role="returnvalues">
  &reftitle.returnvalues;
  <para>
   一意な識別子を文字列で返します。
  </para>
 </refsect1>

 <refsect1 role="examples">
  &reftitle.examples;
  <para>
   一意な ID またはトークンが必要な場合、そして、ネットワーク経由で
   ユーザにそのトークンを渡そうとする場合（例えば、セッションクッキー）、
   次の例のようにするのが推奨されます。
  </para>
  <para>
   この例は、極めて予測困難な 32 文字の ID（128 ビット十六進数）
   を作成します。
   <example>
    <title><function>uniqid</function> の例</title>
    <programlisting role="php">
<![CDATA[
<?php
// 接頭辞なし
// PHP 5 以降でのみ動作します
$token = md5(uniqid());

// よりよい、推測しにくい方法
$better_token = md5(uniqid(mt_rand(), true));
?>
]]>
    </programlisting>
   </example>
  </para>
 </refsect1>

 <refsect1 role="changelog">
  &reftitle.changelog;
  <para>
   <informaltable>
    <tgroup cols="2">
     <thead>
      <row>
       <entry>&Version;</entry>
       <entry>&Description;</entry>
      </row>
     </thead>
     <tbody>
      <row>
       <entry>5.0.0</entry>
       <entry>
        パラメータ <parameter>prefix</parameter> が必須ではなくなりました。
       </entry>
      </row>
      <row>
       <entry>4.3.1</entry>
       <entry>
        <parameter>prefix</parameter> の制限が 114 文字までではなくなりました。
       </entry>
      </row>
     </tbody>
    </tgroup>
   </informaltable>
  </para>
 </refsect1>

 <refsect1 role="notes">
  &reftitle.notes;
  <note>
   <para>
    Cygwin でこの関数を動作させるには、
    <parameter>more_entropy</parameter> を
    &true; にする必要があります。
   </para>
  </note>
 </refsect1>

</refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
