<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision: 1.18 $ -->
<!-- EN-Revision: 1.30 Maintainer: takagi Status: ready -->
<!-- CREDITS: hirokawa,shimooka -->

 <sect1 xml:id="language.oop5.overloading" xmlns="http://docbook.org/ns/docbook">
  <title>オーバーロード</title>

  <para>
   PHP におけるオーバーロード機能は、
   メンバーやメソッドを動的に
   <quote>作成する</quote> ための手法です。
   これらの動的エンティティは、マジックメソッドを用いて処理されます。
   マジックメソッドは、クラス内でさまざまなアクションに対して用意することができます。
  </para>

  <para>
   オーバーロードメソッドが起動するのは、
   宣言されていないメンバーやメソッドを操作しようとしたときです。
   また、現在のスコープからは
   <link linkend="language.oop5.visibility">アクセス不能な</link>
   メンバーやメソッドを操作しようとしたときにも起動します。
   このセクションでは、これらの (宣言されていない、
   あるいは現在のスコープからはアクセス不能な) メンバーやメソッドのことを
   <quote>アクセス不能メンバー</quote> および
   <quote>アクセス不能メソッド</quote>
   と表記することにします。
  </para>

  <para>
<!--
Test scripts indicate otherwise.
Will communicate with internals to see what's up.
http://bugs.php.net/bug.php?id=43924
-->
   オーバーロードメソッドは、すべて <literal>public</literal>
   で定義しなければなりません。
  </para>

  <note>
   <para>
    これらのマジックメソッドの引数は、
    <link linkend="functions.arguments.by-reference">参照渡し</link>
    とすることはできません。
   </para>
  </note>

  <note>
   <para>
    PHP における <quote>オーバーロード</quote>
    の解釈は、他の多くのオブジェクト指向言語とは異なります。
    一般的に「オーバーロード」とは、
    「名前は同じだけれども引数の数や型が異なるメソッドを複数用意できる」
    という機能のことを指します。
   </para>
  </note>


  <sect2 xml:id="language.oop5.overloading.changelog">
   &reftitle.changelog;
   <para>
    <informaltable>
     <tgroup cols="2">
      <thead>
       <row>
        <entry>&Version;</entry>
        <entry>&Description;</entry>
       </row>
      </thead>
      <tbody>
       <row>
        <entry>5.1.0</entry>
        <entry>
         <literal>__isset()</literal> と <literal>__unset()</literal>
         が追加されました。
        </entry>
       </row>
       <row>
        <entry>5.3.0</entry>
        <entry>
         <literal>__callStatic()</literal> が追加されました。
        </entry>
       </row>
      </tbody>
     </tgroup>
    </informaltable>
   </para>
  </sect2>


  <sect2 xml:id="language.oop5.overloading.members">
   <title>メンバーのオーバーロード</title>

   <methodsynopsis>
    <type>void</type><methodname>__set</methodname>
    <methodparam><type>string</type><parameter>name</parameter></methodparam>
    <methodparam><type>mixed</type><parameter>value</parameter></methodparam>
   </methodsynopsis>
   <methodsynopsis>
    <type>mixed</type><methodname>__get</methodname>
    <methodparam><type>mixed</type><parameter>name</parameter></methodparam>
   </methodsynopsis>
   <methodsynopsis>
    <type>bool</type><methodname>__isset</methodname>
    <methodparam><type>string</type><parameter>name</parameter></methodparam>
   </methodsynopsis>
   <methodsynopsis>
    <type>void</type><methodname>__unset</methodname>
    <methodparam><type>string</type><parameter>name</parameter></methodparam>
   </methodsynopsis>

   <para>
    <literal>__set()</literal> は、
    アクセス不能メンバーへデータを書き込む際に実行されます。
   </para>

   <para>
    <literal>__get()</literal> は、
    アクセス不能メンバーからデータを読み込む際に使用します。
   </para>

   <para>
    <literal>__isset()</literal> は、
    <function>isset</function> あるいは <function>empty</function>
    をアクセス不能メンバーに対して実行したときに起動します。
   </para>

   <para>
    <literal>__unset()</literal> は、
    <function>unset</function>
    をアクセス不能メンバーに対して実行したときに起動します。
   </para>

   <para>
    引数 <varname>$name</varname> は、
    操作しようとしたメンバーの名前です。
    <literal>__set()</literal> メソッドの引数
    <varname>$value</varname> は、
    <varname>$name</varname> に設定しようとした値となります。
   </para>

   <para>
    メンバーのオーバーロードはオブジェクトのコンテキストでのみ動作します。
    これらのマジックメソッドは、静的コンテキストでは起動しません。
    したがって、これらのメソッドは
    <link linkend="language.oop5.static">static</link>
    宣言することはできません。
   </para>

   <example>
    <title>__get, __set, __isset, __unset を使ったオーバーロードの例</title>
    <programlisting role="php">
<![CDATA[
<?php
class MemberTest {
    /**  オーバーロードされるデータの場所  */
    private $data = array();

    /**  宣言されているメンバーにはオーバーロードは起動しません */
    public $declared = 1;

    /**  クラス内からアクセスした場合にはオーバーロードは起動しません  */
    private $hidden = 2;

    public function __set($name, $value) {
        echo "Setting '$name' to '$value'\n";
        $this->data[$name] = $value;
    }

    public function __get($name) {
        echo "Getting '$name'\n";
        if (array_key_exists($name, $this->data)) {
            return $this->data[$name];
        }

        $trace = debug_backtrace();
        trigger_error(
            'Undefined property: ' . $name .
            ' in ' . $trace[0]['file'] .
            ' on line ' . $trace[0]['line'],
            E_USER_NOTICE);
        return null;
    }

    /**  PHP 5.1.0 以降 */
    public function __isset($name) {
        echo "Is '$name' set?\n";
        return isset($this->data[$name]);
    }

    /**  PHP 5.1.0 以降 */
    public function __unset($name) {
        echo "Unsetting '$name'\n";
        unset($this->data[$name]);
    }

    /**  マジックメソッドではありません。単なる例として示しています  */
    public function getHidden() {
        echo "'hidden' visible here so __get() not used\n";
        return $this->hidden;
    }
}


echo "<pre>\n";

$obj = new MemberTest;

$obj->a = 1;
echo $obj->a . "\n";

var_dump(isset($obj->a));
unset($obj->a);
var_dump(isset($obj->a));

echo $obj->declared . "\n";
echo $obj->getHidden() . "\n";
echo $obj->hidden . "\n";
?>
]]>
    </programlisting>
    &example.outputs;
    <screen role="php">
<![CDATA[
Setting 'a' to '1'
Getting 'a'
1
Is 'a' set?
bool(true)
Unsetting 'a'
Is 'a' set?
bool(false)
1
'hidden' visible here so __get() not used
2
Getting 'hidden'


Notice:  Undefined property: hidden in <file> on line 64 in <file> on line 28
]]>
    </screen>

   </example>
  </sect2>

  <sect2 xml:id="language.oop5.overloading.methods">
   <title>メソッドのオーバーロード</title>

   <methodsynopsis>
    <type>mixed</type><methodname>__call</methodname>
    <methodparam><type>string</type><parameter>name</parameter></methodparam>
    <methodparam><type>array</type><parameter>arguments</parameter></methodparam>
   </methodsynopsis>
   <methodsynopsis>
    <type>mixed</type><methodname>__callStatic</methodname>
    <methodparam><type>string</type><parameter>name</parameter></methodparam>
    <methodparam><type>array</type><parameter>arguments</parameter></methodparam>
   </methodsynopsis>

   <para>
    <literal>__call()</literal> は、
    アクセス不能メソッドをオブジェクトのコンテキストで実行したときに起動します。
   </para>

   <para>
    <literal>__callStatic()</literal> は、
    アクセス不能メソッドを静的コンテキストで実行したときに起動します。
   </para>

   <para>
    引数 <varname>$name</varname> は、
    コールしようとしたメソッドの名前です。
    引数 <varname>$arguments</varname>
    は配列で、メソッド <varname>$name</varname>
    に渡そうとしたパラメータが格納されます。
   </para>

   <example>
    <title>__call および ___callStatic による、インスタンス化されたメソッドのオーバーロードの例</title>
    <programlisting role="php">
  <![CDATA[
<?php
class MethodTest {
    public function __call($name, $arguments) {
        // 注意: $name は大文字小文字を区別します
        echo "Calling object method '$name' "
             . implode(', ', $arguments). "\n";
    }

    /**  PHP 5.3.0 以降 */
    public static function __callStatic($name, $arguments) {
        // 注意: $name は大文字小文字を区別します
        echo "Calling static method '$name' "
             . implode(', ', $arguments). "\n";
    }
}

$obj = new MethodTest;
$obj->runTest('in object context');

MethodTest::runTest('in static context');  // PHP 5.3.0 以降
?>
]]>
    </programlisting>
    &example.outputs;
    <screen role="php">
<![CDATA[
Calling object method 'runTest' in object context
Calling static method 'runTest' in static context
]]>
    </screen>
   </example>

  </sect2>

 </sect1>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
