<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision$ -->
<!-- EN-Revision: fa1ad00f0059f3a33252a6921079eb40319f16e2 Maintainer: takagi Status: ready -->

<refentry xml:id="amqpqueue.ack" xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
 <refnamediv>
  <refname>AMQPQueue::ack</refname>
  <refpurpose>メッセージの受信を受け付ける</refpurpose>
 </refnamediv>

 <refsect1 role="description">
  &reftitle.description;
  <methodsynopsis>
   <modifier>public</modifier> <type>bool</type><methodname>AMQPQueue::ack</methodname>
   <methodparam><type>int</type><parameter>delivery_tag</parameter></methodparam>
   <methodparam choice="opt"><type>int</type><parameter>flags</parameter><initializer>NULL</initializer></methodparam>
  </methodsynopsis>
  <para>
    <methodname>AMQPQueue::get</methodname> あるいは <methodname>AMQPQueue::consume</methodname> で
    <constant>AMQP_NOACK</constant> フラグつきで取得したメッセージの受信を認めます。
  </para>
 </refsect1>

 <refsect1 role="parameters">
  &reftitle.parameters;
  <para>
   <variablelist>
    <varlistentry>
     <term><parameter>delivery_tag</parameter></term>
     <listitem>
      <para>
       受信を認めるメッセージ配送タグ。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><parameter>flags</parameter></term>
     <listitem>
      <para>
       渡せるフラグは <constant>AMQP_MULTIPLE</constant> のみです。
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </para>
 </refsect1>

 <refsect1 role="returnvalues">
  &reftitle.returnvalues;
  <para>
   &return.success;
  </para>
 </refsect1>

 <refsect1 role="examples">
  &reftitle.examples;
  <para>
   <example>
    <title><methodname>AMQPQueue::ack</methodname> と <methodname>AMQPQueue::get</methodname> の例</title>
    <programlisting role="php">
<![CDATA[
<?php

/* デフォルトの設定で接続を作成します */
$connection = new AMQPConnection();
$connection->connect();

/* キューオブジェクトを作成します */
$queue = new AMQPQueue($connection);

// キューを宣言します
$queue->declare('myqueue');

// 次のメッセージを取得しますが、配送済みにはしません
$message = $queue->get(AMQP_NOACK);

echo $message['msg'];

// メッセージを受信したことを知らせます
$queue->ack($message['delivery_tag']);

?>
]]>
    </programlisting>
   </example>
   <example>
    <title><methodname>AMQPQueue::ack</methodname> と <methodname>AMQPQueue::consume</methodname> の例</title>
    <programlisting role="php">
<![CDATA[
<?php

/* デフォルトの設定で接続を作成します */
$connection = new AMQPConnection();
$connection->connect();

/* キューオブジェクトを作成します */
$queue = new AMQPQueue($connection);

// キューを宣言します
$queue->declare('myqueue');

$options = array(
    'min' => 1,
    'max' => 10,
    'ack' => false
);

// メッセージを取得しますが、配送済みにはしません
$messages = $queue->consume($options);

foreach ($messages as $message) {
    echo $message['message_body'];
    // メッセージを受信したことを知らせます
    $queue->ack($message['delivery_tag']);
}

?>
]]>
    </programlisting>
   </example>
  </para>
 </refsect1>
</refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"~/.phpdoc/manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
