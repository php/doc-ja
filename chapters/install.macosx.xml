<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision: 1.5 $ -->
<!-- sync: 1.5 -->
<sect1 id="install.macosx">
 <title>Mac OS X へのインストール</title>
 <para>
  本節は、PHPをMac OS X Server上にインストールする際の注意事項とコツを説明し
  ます。
 </para>
 <sect2 id="install.macosx.packages">
  <title>パッケージの使用法</title>
  <simpara>
   数は少ないですが、Mac OS X 用にパッケージ化され、コンパイルされた
   PHPが存在します。これにより標準的な設定を構築することが非常に楽に
   なりますが、(セキュアサーバまたは別のデータベースドライバのような)
   異なる機能が必要な場合には、PHPやWebサーバを自分で構築する必要が
   あるかもしれません。ソフトウエアの構築およびコンパイルに不慣れな場
   合、必要とする機能を有するパッケージ版のPHPが既に構築されていない
   かどうかを調べることを推奨します。
  </simpara>
 </sect2>
 <sect2 id="install.macosx.compile">
  <title>Mac OS X サーバへのインストール</title>
  <simpara>
   Mac OS Xには僅かに異なる二つのバージョン、つまり、クライアントと
   サーバがあります。以下の説明は、OS Xサーバに関するものです。
  </simpara>
  <formalpara id="install.macosx.compile.example">
   <title>Mac OS X server install</title>
   <para>
    <orderedlist>
     <listitem>
      <simpara>ApacheとPHPの最新版を入手</simpara>
     </listitem>
     <listitem>
      <para>
       これらのtarを展開し、Apacheの<command>configure</command>
       プログラムを以下のように実行
       <programlisting role="shell">
<![CDATA[
./configure --exec-prefix=/usr \
--localstatedir=/var \
--mandir=/usr/share/man \
--libexecdir=/System/Library/Apache/Modules \
--iconsdir=/System/Library/Apache/Icons \
--includedir=/System/Library/Frameworks/Apache.framework/Versions/1.3/Headers \
--enable-shared=max \
--enable-module=most \
--target=apache
]]>
       </programlisting>
      </para>
     </listitem>
     <listitem>
      <para>
       コンパイラに最適化を行わせたい場合には、次の行を追加できます。
       <programlisting role="shell">
<![CDATA[
setenv OPTIM=-O2
]]>
       </programlisting>
      </para>
     </listitem>
     <listitem>
      <para>
       PHP4ソースディレクトリで、configure を行います。
       <programlisting role="shell">
<![CDATA[
./configure --prefix=/usr \
    --sysconfdir=/etc \
    --localstatedir=/var \
    --mandir=/usr/share/man \
    --with-xml \
    --with-apache=/src/apache_1.3.12
]]>
       </programlisting>
       他に追加するもの(MySQL、GD等)がある場合、必ずここでこれらを追加する
       ようにして下さい。<option role="configure">--with-apache</option>
       文字列に関しては、Apacheソースのディレクトリを
       <filename class="directory">/src/apache_1.3.12</filename>のように指定して下さい。
      </para>
     </listitem>
     <listitem>
      <simpara>
       <command>make</command>および<command>make install</command>を実行します。
       これにより、Apacheソースディレクトリに
       <filename class="directory">src/modules/php4</filename>の下の
       ディレクトリが追加されます    
      </simpara>
     </listitem>
     <listitem>
      <para>
       ここで、Apacheを再設定し、 PHP4を再構築します
       <programlisting role="shell">
<![CDATA[
./configure --exec-prefix=/usr \
--localstatedir=/var \
--mandir=/usr/share/man \
--libexecdir=/System/Library/Apache/Modules \
--iconsdir=/System/Library/Apache/Icons \
--includedir=/System/Library/Frameworks/Apache.framework/Versions/1.3/Headers \
--enable-shared=max \
--enable-module=most \
--target=apache \
--activate-module=src/modules/php4/libphp4.a
]]>
       </programlisting>
       libmodphp4.a が期限切れというメッセージが出力されるかもしれません。
       この場合、Apacheソースディレクトリの配下の
       <filename class="directory">src/modules/php4</filename>
       ディレクトリに行き、以下のコマンドを実行します:
       <command>ranlib libmodphp4.a</command>
       次にApacheソースディレクトリのルートに戻り、
       上記の<command>configure</command>コマンドを再び実行します。
       これによりリンクテーブルが最新になります。再度、
       <command>make</command>および<command>make install</command>を
       実行します。
      </para>
     </listitem>
     <listitem>
      <simpara>
       Copy and rename the <filename>php.ini-dist</filename> file to your
       <filename class="directory">bin</filename> directory from your PHP 4
       source directory: 
       <userinput>cp php.ini-dist /usr/local/bin/php.ini</userinput>
       or (if your don't have a local directory)
       <userinput>cp php.ini-dist /usr/bin/php.ini</userinput>.

       <filename>php.ini-dist</filename>ファイルをPHP4ソースディレクトリから 
       <filename class="directory">bin</filename>ディレクトリにコピーし、
       リネームします:
       <userinput>cp php.ini-dist /usr/local/bin/php.ini</userinput>
       または(localディレクトリが無い場合)
       <userinput>cp php.ini-dist /usr/bin/php.ini</userinput>

      </simpara>
     </listitem>
    </orderedlist>
   </para>
  </formalpara>
  <!--
  <simpara>
  TODO: Someone with Mac OS X skills needs to improve this DOC
        Also, these links are 404 and outdated anyways and
        stepwise doesn't support PHP anymore.
        See also bug #14732

   Other examples for
   <ulink url="&url.stepwise.macosx-client;">Mac OS X client</ulink>
   and
   <ulink url="&url.stepwise.macosx-client;">Mac OS X server</ulink>
   are available at <ulink url="&url.stepwise;">Stepwise</ulink>.
  </simpara>
  -->
  
 </sect2>
 <sect2 id="install.macosx.client">
  <title>MacOS X用クライアントのコンパイル</title>
  <simpara>
   以下のヒントは、<ulink url="&url.macosx;">Marc Liyanage</ulink>
   氏に提供して頂いたものです。
  </simpara>
  <simpara>
   Apache Webサーバ用PHPモジュールがMac OS Xに含まれています。
   このバージョンには、 データベースMySQLとPostgreSQLのサポートが含まれています。
 </simpara>
  <simpara>
   注意:以下の手順を行う際は、Apache Webサーバをダウンさせる可能性
   があるので注意して下さい!
  </simpara>
  <para>
   以下のインストール手順を実行します。
   <orderedlist>
    <listitem>
     <simpara>
      ターミナルウインドウをオープン
     </simpara>
    </listitem>
    <listitem>
     <simpara>
      <userinput>wget http://www.diax.ch/users/liyanage/software/macosx/libphp4.so.gz</userinput>,
      を入力し、ダウンロード完了を待つ。
     </simpara>
    </listitem>
    <listitem>
     <simpara>
      <userinput>gunzip libphp4.so.gz</userinput>を入力
     </simpara>
    </listitem>
    <listitem>
     <simpara>
      <userinput>sudo apxs -i -a -n php4 libphp4.so</userinput> を入力
     </simpara>
    </listitem>
    <listitem>
     <para>
      次に <userinput>sudo open -a TextEdit /etc/httpd/httpd.conf</userinput>を
      入力すると、TextEditがWebサーバの設定ファイルをオープンします。ファイルの
      末尾の方にある以下の２行を探して下さい。(検索コマンドを使用して下さい)
      <programlisting role="apache">
<![CDATA[
#AddType application/x-httpd-php .php 
#AddType application/x-httpd-php-source .phps
]]>
      </programlisting>
       ハッシュ記号(<literal>#</literal>)を削除し、ファイルを保存、
       TextEditを終了して下さい。
      </para>
    </listitem>
    <listitem>
     <simpara>
      最後に、Webサーバを再起動するために
      <literal>sudo apachectlgraceful</literal>と入力して下さい。 
     </simpara>
    </listitem>
   </orderedlist>
  </para>
  <para>
   この時点でPHPが動作しているはずです。<filename>test.php</filename>という
   名前のファイルを<filename class="directory">Sites</filename>フォルダに
   作成して下さい。このファイルには、次の行を
   書いて下さい: <literal>&lt;?php phpinfo() ?&gt;</literal>
  </para>
  <para>
   次に<literal>127.0.0.1/~your_username/test.php</literal>をWebブラウザから
   オープンして下さい。PHPモジュールに関する情報を含むステータステーブルが
   表示されるはずです。
  </para>
 </sect2>
</sect1>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
