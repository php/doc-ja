<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision: 1.8 $ -->
 <chapter id="features.remote-files">
  <title>リモートファイルの使用</title>

  <para>
   php.iniで<parameter>allow_url_fopen</parameter>を有効にした場合、
   パラメータとしてファイル名をとる関数の多くでHTTPおよびFTPのURL
   を使用することができます。加えて、<function>include</function>,
   <function>include_once</function>, <function>require</function>,
   <function>require_once</function> 命令でURLを使用することができます。
  </para>
  <note>
   <para>
    In PHP 4.0.3 and older, in order to use URL wrappers, you were required
    to configure PHP using the configure option
    <option>--enable-url-fopen-wrapper</option>.
   </para>
  </note>
  <para>
   <note>
    <para>
     Windows上では、リモートファイルに <function>include</function> および
     <function>require</function> 文を使用することはできません。
    </para>
   </note>
  </para>
  <para>
   例えば、リモートWebサーバーにファイルをオープンし、データを出力、デー
   タベースクエリーに使用するか、単にWebサイトのスタイルに合わせて出力
   を行うことが可能です。
  </para>
  <para>
   <example>
    <title>リモートページのタイトルを得る</title>
    <programlisting role="php">
<![CDATA[
<?php
$file = fopen("http://www.php.net/", "r");
if (!$file) {
  echo "<p>Unable to open remote file.\n";
  exit;
}
while (!feof($file)) {
  $line = fgets($file, 1024);
  /* タイトルとタグが同じ行にある場合のみ動作します。 */
  if (eregi("<title>(.*)</title>", $line, $out)) {
    $title = $out[1];
    break;
  }
}
fclose($file);
?>
]]>
    </programlisting>
   </example>
  </para>
  <para>
   You can also write to files on an FTP server (provided that you
   have connected as a user with the correct access rights). You
   can only create new files using this method; if you try to overwrite
   a file that already exists, the <function>fopen</function> will
   fail.
  </para>
  <para>
   'anonymous'以外のユーザーで接続を行う場合、URLの中で
   'ftp://user:password@ftp.example.com/path/to/file' のように
   ユーザー名(そして多分パスワードも)指定する必要があります。
   (Basic認証を要求された際にHTTP経由でファイルをアクセスする場合と
   同じ種類の構文を使用することができます。)
  </para>
  <para>
   <example>
    <title>リモートサーバーにデータを保存する</title>
    <programlisting role="php">
<![CDATA[
<?php
$file = fopen ("ftp://ftp.example.com/incoming/outputfile", "w");
if (!$file) {
    echo "<p>Unable to open remote file for writing.\n";
    exit;
}
/* データをここに書きます。 */
fputs ($file, $_SERVER['HTTP_USER_AGENT'] . "\n");
fclose ($file);
?>
]]>
    </programlisting>
   </example>
  </para>
  <para>
   <note>
    <para>
     上の例からリモートログに書きこむためにこの手法を使用することを考えるかも
     しれません。
     Unfortunately
     that would not work because the <function>fopen</function> call will
     fail if the remote file already exists.
     分散ロギングのようなことを行うには、
     <function>syslog</function> の使用を考えてみて下さい。
    </para>
   </note>
  </para>

 </chapter>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
-->
