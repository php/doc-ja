<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision: 1.5 $ -->
<!-- sync: 1.5 (assured) -->
 <reference id="ref.w32api">
  <title>W32api関数</title>
  <titleabbrev>W32api関数</titleabbrev>

  <partintro>
   <section id="w32api.intro">
    &reftitle.intro;
    <para>
     この拡張モジュールは、DLLへの一般的な拡張APIです。この拡張モジュー
     ルはもともとPHPからWin32 APIにアクセスできるようにするために作
     成されましたが、他のDLLによりエクスポートされている他の関数にもア
     クセスすることができます。
    </para>
    <para>
     現在、サポートされている型は一般のPHP型(文字列, boolean, float,
     整数, NULL)と<function>w32api_deftype</function>で定義した型です。
    </para>
    &warn.experimental;
   </section>

   <section id="w32api.requirements">
    &reftitle.required;
    <para>
     この拡張モジュールはWindowsシステムでのみ動作します。
    </para>
   </section>
   
   <section id="w32api.installation">
    &reftitle.install;
    &no.install;
   </section>
   
   <section id="w32api.configuration">
    &reftitle.runtime;
    &no.config;
   </section>

   <section id="w32api.resources">
    &reftitle.resources;
    <para>
     この拡張モジュールは、1種類のリソース型を定義し、ユーザ定義型で使
     用されます。このリソースの名前は、<literal>"dynaparm"</literal>で
     す。
    </para>
   </section>

   &reference.w32api.constants;
   
   <section id="w32api.examples">
    &reftitle.examples;
    <para>
     以下の例は、システムの実行時間を取得し、メッセージボックスに表示
     するものです。
    </para>
    <example>
     <title>uptimeを取得し、メッセージボックスに表示する</title>
     <programlisting role="php">
<![CDATA[
<?php
// Define constants needed, taken from
// Visual Studio/Tools/Winapi/WIN32API.txt
define("MB_OK", 0);

// Load the extension in
dl("php_w32api.dll");

// Register the GetTickCount function from kernel32.dll
w32api_register_function("kernel32.dll", 
                         "GetTickCount",
                         "long");
                         
// Register the MessageBoxA function from User32.dll
w32api_register_function("User32.dll",
                         "MessageBoxA",
                         "long");

// Get uptime information
$ticks = GetTickCount();

// Convert it to a nicely displayable text
$secs  = floor($ticks / 1000);
$mins  = floor($secs / 60);
$hours = floor($mins / 60);

$str = sprintf("You have been using your computer for:".
                "\r\n %d Milliseconds, or \r\n %d Seconds".
                "or \r\n %d mins or\r\n %d hours %d mins.",
                $ticks,
                $secs,
                $mins,
                $hours,
                $mins - ($hours*60));

// Display a message box with only an OK button and the uptime text
MessageBoxA(NULL, 
            $str, 
            "Uptime Information", 
            MB_OK);
?>
]]>
     </programlisting>
    </example>
   </section>
  </partintro>

&reference.w32api.functions;

 </reference>
<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->

