<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision$ -->
<!-- EN-Revision: af4410a7e15898c3dbe83d6ea38246745ed9c6fb Maintainer: shimooka Status: ready -->
<!-- CREDITS: hirokawa -->
<refentry xml:id="function.xslt-process" xmlns="http://docbook.org/ns/docbook">
 <refnamediv>
  <refname>xslt_process</refname>
  <refpurpose><acronym>XSLT</acronym> による変換を行う</refpurpose>
 </refnamediv>
 
 <refsect1 role="description">
  &reftitle.description;
  <methodsynopsis>
   <type>mixed</type><methodname>xslt_process</methodname>
   <methodparam><type>resource</type><parameter>xh</parameter></methodparam>
   <methodparam><type>string</type><parameter>xmlcontainer</parameter></methodparam>
   <methodparam><type>string</type><parameter>xslcontainer</parameter></methodparam>
   <methodparam choice="opt"><type>string</type><parameter>resultcontainer</parameter></methodparam>
   <methodparam choice="opt"><type>array</type><parameter>arguments</parameter></methodparam>
   <methodparam choice="opt"><type>array</type><parameter>parameters</parameter></methodparam>
  </methodsynopsis>
  <para>
   <function>xslt_process</function>関数は、<acronym>XSLT</acronym> 拡張モジュールの中心となる関数です。
   ほとんど全ての型の入力ソース (コンテナ) を用いて <acronym>XSLT</acronym> 変換を実行可能です。
   これを実現しているのが、引数バッファです。
   引数バッファとは、<productname>Sablotron</productname> <acronym>XSLT</acronym> プロセッサ
   (現在、この拡張モジュールがサポートする惟一の <acronym>XSLT</acronym> プロセッサ)
   から得た概念です。
   入力コンテナは処理する文章を '含んでいる' ファイル名がデフォルトです。
  </para>
 </refsect1>

 <refsect1 role="parameters">
  &reftitle.parameters;
  <para>
   <variablelist>
    <varlistentry>
     <term><parameter>xh</parameter></term>
     <listitem>
      <para>
       <function>xslt_create</function> で作成した
       <acronym>XSLT</acronym> プロセッサリンク ID。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><parameter>xmlcontainer</parameter></term>
     <listitem>
      <para>
       <acronym>XML</acronym> ファイルへのパス、あるいは <acronym>XML</acronym> 引数用のプレースホルダ。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><parameter>xslcontainer</parameter></term>
     <listitem>
      <para>
       <acronym>XSL</acronym> ファイルへのパス、あるいは <acronym>XML</acronym> 引数用のプレースホルダ。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><parameter>resultcontainer</parameter></term>
     <listitem>
      <para>
       結果コンテナは、変換された文章のためのファイル名が
       デフォルトです。もし結果コンテナが指定されていない場合 - 例えば &null; -
       、結果が返されます。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><parameter>arguments</parameter></term>
     <listitem>
      <para>
       <acronym>XML</acronym> や <acronym>XSLT</acronym> のファイル名を <function>xslt_process</function>
       関数に指定するかわりに、&quot;引数プレースホルダ&quot;
       を使用することもできます。これは、配列
       <parameter>arguments</parameter> に指定した内容で置き換えられます。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><parameter>parameters</parameter></term>
     <listitem>
      <para>
       任意のトップレベルパラメータの配列。これが XSLT
       ドキュメントに渡されます。XSL ファイル内でこれらのパラメータにアクセスするには
       <literal>&lt;xsl:param name=&quot;parameter_name&quot;&gt;</literal> を使用します。
       パラメータは UTF-8 でエンコードする必要があります。
       その値は文字列として <productname>Sablotron</productname>
       プロセッサが処理します。つまり、<acronym>XSLT</acronym> ドキュメントのパラメータには
       ノードセットを渡すことはできないということです。
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </para>
  <para>
   また、<parameter>arguments</parameter>
   配列を通じてコンテナを設定することができます (以下参照)。
  </para>
 </refsect1>

 <refsect1 role="returnvalues">
  &reftitle.returnvalues;
  <para>
   &return.success; 結果コンテナを指定していない場合は結果を返します。
  </para>
 </refsect1>

 <refsect1 role="changelog">
  &reftitle.changelog;
  <para>
   <informaltable>
    <tgroup cols="2">
     <thead>
      <row>
       <entry>&Version;</entry>
       <entry>&Description;</entry>
      </row>
     </thead>
     <tbody>
      <row>
       <entry>4.0.6</entry>
       <entry>
        この関数は <parameter>xmlcontainer</parameter>
        もしくは <parameter>xslcontainer</parameter>
        で XML 文字列を受け付けなくなりました。
        XML を含む文字列を渡すと、0.95とそれ以降の Sablotron
        バージョンでセグメンテーションフォルトを引き起こします。
       </entry>
      </row>
     </tbody>
    </tgroup>
   </informaltable>
  </para>
 </refsect1>
 
 <refsect1 role="examples">
  &reftitle.examples;
  <para>
   <function>xslt_process</function>関数で変換する最も簡単な方法は、
   <acronym>XML</acronym> ファイルを <acronym>XSLT</acronym>
   ファイルで変換し、結果を新しい <acronym>XML</acronym> ドキュメント
   (または HTML ドキュメント) を含む3番目のファイルに出力することです。
   これを <productname>Sablotron</productname> で行うのはとても簡単です。
  </para>
  <example>
   <title>
    <acronym>XML</acronym> ファイルと <acronym>XSL</acronym> ファイルを新規
    <acronym>XML</acronym> ファイルに変換するために
    <function>xslt_process</function> を使用する
   </title>
   <programlisting role="php">
<![CDATA[
<?php

// 新規 XSLT プロセッサを割り当てる
$xh = xslt_create();

// 文章を処理する
if (xslt_process($xh, 'sample.xml', 'sample.xsl', 'result.xml')) {
    echo "SUCCESS, sample.xml was transformed by sample.xsl into result.xml";
    echo ", result.xml has the following contents\n<br />\n";
    echo "<pre>\n";
    readfile('result.xml');
    echo "</pre>\n";
} else {
    echo "Sorry, sample.xml could not be transformed by sample.xsl into";
    echo "  result.xml the reason is that " . xslt_error($xh) . " and the ";
    echo "error code is " . xslt_errno($xh);
}

xslt_free($xh);

?>
]]>
   </programlisting>
  </example>
  <para>
   この機能は優れていますが、特にWeb環境では、結果を直接出力したい場
   合があります。そこで、<function>xslt_process</function>の3番目の
   引数を省略した場合(またはその引数にNULL値を指定した場合)、ファイ
   ルに書き込む替わりに自動的に <acronym>XSLT</acronym> 変換後の出力を返します。
  </para>
  <para>
   <example>
   <title>
    <acronym>XML</acronym> ファイルと <acronym>XSL</acronym> ファイルを結果XMLデータを含む変数に変換するために
    <function>xslt_process</function> を使用する
   </title>
   <programlisting role="php">
<![CDATA[
<?php

// 新規 XSLT プロセッサを割り当てる
$xh = xslt_create();

// 文章を処理し、変数 $result に結果を返す
$result = xslt_process($xh, 'sample.xml', 'sample.xsl');
if ($result) {
    echo "SUCCESS, sample.xml was transformed by sample.xsl into the \$result";
    echo " variable, the \$result variable has the following contents\n<br />\n";
    echo "<pre>\n";
    echo $result;
    echo "</pre>\n";
} else {
    echo "Sorry, sample.xml could not be transformed by sample.xsl into";
    echo "  the \$result variable the reason is that " . xslt_error($xh);
    echo " and the error code is " . xslt_errno($xh);
}

xslt_free($xh);

?>
]]>
    </programlisting>
   </example>
  </para>
  <para>
   上の二つのケースは、XSLT変換の最も簡単な場合です。これは、多くの
   場合には通用しますが、時々、データベースまたはソケットのような外
   部ソースから <acronym>XML</acronym> と <acronym>XSLT</acronym> コードを取得する場合があります。このような場
   合、<acronym>XML</acronym> または <acronym>XSLT</acronym> データを変数に有することになります。
   実用アプリケーションでは、これらをファイルにダンプする際のオーバー
   ヘッドは大きいと言えます。このような場合こそ、<acronym>XSLT</acronym>
   &quot;argument&quot; 構文が役に立ちます。
   <function>xslt_process</function>関数の <acronym>XML</acronym> および <acronym>XSLT</acronym> 引数としてファ
   イルの替わりに引数配列(<function>xslt_process</function>関数の5番
   目のパラメータ)で指定した値に置換される&quot;argument place
   holders&quot;を指定することが可能です。
   以下にファイルを全く使用せずに <acronym>XML</acronym> および <acronym>XSLT</acronym> を結果変数に処理する例
   を示します。
  </para>
  <para>
   <example>
   <title>
    <acronym>XML</acronym> データを含む変数と <acronym>XSLT</acronym> データを含む変数を <acronym>XML</acronym> データ出力結果を
    含む変数に変換するために<function>xslt_process</function>を使用
    する
   </title>
   <programlisting role="php">
<![CDATA[
<?php
// $xml と $xsl は XML データと XSL データを含む

$arguments = array(
     '/_xml' => $xml,
     '/_xsl' => $xsl
);

// 新規 XSLT プロセッサを割り当てる
$xh = xslt_create();

// 文章を処理する
$result = xslt_process($xh, 'arg:/_xml', 'arg:/_xsl', NULL, $arguments);
if ($result) {
    echo "SUCCESS, sample.xml was transformed by sample.xsl into the \$result";
    echo " variable, the \$result variable has the following contents\n<br />\n";
    echo "<pre>\n";
    echo $result;
    echo "</pre>\n";
} else {
    echo "Sorry, sample.xml could not be transformed by sample.xsl into";
    echo "  the \$result variable the reason is that " . xslt_error($xh);
    echo " and the error code is " . xslt_errno($xh);
}
xslt_free($xh);
?>
]]>
    </programlisting>
   </example>
  </para>
  <para>
   <example>
    <title>PHP 変数を XSL ファイルに渡す</title>
    <programlisting role="php">
<![CDATA[
<?php

// XML 文字列
$xml = '<?xml version="1.0"?>
<para>
 change me
</para>';

// XSL 文字列
$xsl = '
<xsl:stylesheet version="1.0" xmlns:xsl="http://www.w3.org/1999/XSL/Transform">
<xsl:output method="html" encoding="ISO-8859-1" indent="no"
 omit-xml-declaration="yes"  media-type="text/html"/>
 <xsl:param name="myvar"/>
 <xsl:param name="mynode"/>
 <xsl:template match="/">
My PHP variable : <xsl:value-of select="$myvar"/><br />
My node set : <xsl:value-of select="$mynode"/>
 </xsl:template>
</xsl:stylesheet>';


$xh = xslt_create();

// 2番目のパラメータは文字列として解釈される
$parameters = array (
  'myvar' => 'test',
  'mynode' => '<foo>bar</foo>'
);

$arguments = array (
  '/_xml' => $xml,
  '/_xsl' => $xsl
);

echo xslt_process($xh, 'arg:/_xml', 'arg:/_xsl', NULL, $arguments, $parameters);

?>
]]>
    </programlisting>
    &example.outputs;
    <screen>
<![CDATA[
My PHP variable : test<br>
My node set : &lt;foo&gt;bar&lt;/foo&gt;
]]>
    </screen>
   </example>
  </para>
 </refsect1>
 
 <refsect1 role="notes">
  &reftitle.notes;
  &note.xslt.windows;
 </refsect1>

</refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"~/.phpdoc/manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
