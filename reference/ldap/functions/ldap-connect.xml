<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision: 1.5 $ -->
<!-- splitted from ./ja/functions/ldap.xml, last change in rev 1.1 -->
<!-- EN-Revision: 1.5 Maintainer: hirokawa Status: ready -->
<!-- CREDITS: takagi -->
  <refentry id="function.ldap-connect">
   <refnamediv>
    <refname>ldap_connect</refname>
    <refpurpose>LDAP サーバへ接続する</refpurpose>
   </refnamediv>
   <refsect1>
    <title>説明</title>
     <methodsynopsis>
      <type>resource</type><methodname>ldap_connect</methodname>
      <methodparam choice="opt"><type>string</type><parameter>hostname</parameter></methodparam>
      <methodparam choice="opt"><type>int</type><parameter>port</parameter></methodparam>
     </methodsynopsis>
    <para>
     成功した場合に正の LDAP リンク ID 、エラーの場合に &false;
     を返します。OpenLDAP 2.x.x を使用している場合は、
     <function>ldap_connect</function> は常に <type>resource</type>
     を返しますが、実際には接続せずにパラメータの初期化だけを行います。
     実際に接続するのは次に ldap_* 関数がコールされた際で、これは
     通常は <function>ldap_bind</function> です。
    </para>
    <para>  
     <function>ldap_connect</function> は、
     <parameter>hostname</parameter> と <parameter>port</parameter> で
     指定された LDAP サーバーへの接続を確立します。
     引数は両方ともオプションです。
     引数が指定されない場合、既に開かれているリンクのリンク ID が
     返されます。
     <parameter>hostname</parameter> のみが指定された場合、
     ポートのデフォルト値は 389 となります。
    </para>
    <para>
     OpenLDAP 2.x.x を使用している場合、ホスト名の代わりに URL を指定する
     ことが可能です。SSL と組み合わせて LDAP を使用するには、SSL サポートを
     を指定して OpenLDAP 2.x.x をコンパイル、PHP の configure で
     SSL を指定し、パラメータ <parameter>hostname</parameter> で
     ldaps://hostname/ を使用してください。<parameter>port</parameter>
     パラメータは、URL を使用する場合には使用されません。
    </para>
    <note>
     <simpara>
       URL および SSL のサポートは、4.0.4 で追加されました。
     </simpara>
    </note>
    <example>
     <title>LDAP サーバに接続する例</title>
     <programlisting role="php">
<![CDATA[
<?php

// LDAP 変数
$ldaphost = "ldap.example.com";  // ldap サーバ
$ldapport = 389;                 // ldap サーバのポート番号

// Connecting to LDAP
$ldapconn = ldap_connect($ldaphost, $ldapport) 
          or die("Could not connect to $ldaphost");

?>
]]>
     </programlisting>
    </example>
    <example>
     <title>LDAP サーバへのセキュアな接続の例</title>
     <programlisting role="php">
<![CDATA[
<?php

// サーバ証明書が証明するホストであることを
// 確認する
$ldaphost = "ldaps://ldap.example.com/";

// LDAP に接続する
$ldapconn = ldap_connect($ldaphost) 
          or die("Could not connect to {$ldaphost}");

?>
]]>
     </programlisting>
    </example>
    <para>
     <function>ldap_bind</function> も参照ください。
    </para>
   </refsect1>
  </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
