<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision: 1.9 $ -->
<!-- EN-Revision: 1.23 Maintainer: hirokawa Status: ready -->
<!-- CREDITS: shimooka -->
<refentry id="function.parse-ini-file">
 <refnamediv>
  <refname>parse_ini_file</refname>
  <refpurpose>設定ファイルをパースする</refpurpose>
 </refnamediv>
 <refsect1>
  <title>説明</title>
  <methodsynopsis>
   <type>array</type><methodname>parse_ini_file</methodname>
   <methodparam><type>string</type><parameter>filename</parameter></methodparam>
   <methodparam choice="opt"><type>bool</type><parameter>process_sections</parameter></methodparam>
  </methodsynopsis>
  <para>
   <function>parse_ini_file</function> は、
   <parameter>filename</parameter> で指定した ini ファイルをロードし、
   連想配列としてその設定値を返します。
   直近の <parameter>process_sections</parameter> パラメータに &true;
   を設定することにより、セクション名と設定が含まれた多次元の配列を
   得ることができます。デフォルトでは、
   <parameter>process_sections</parameter> は &false; です。
  </para>
  <note>
   <para>
    この関数は、&php.ini; ファイルには何もしません。
    このファイルはスクリプトを実行している時には既に処理されています。
    この関数は、アプリケーション個有の設定ファイルを読み込む際に使用
    可能です。
   </para>
  </note>
  <note>
   <para>
    ini ファイル上の値に英数字ではないものがある場合、
    ダブルクォート(")で囲う必要があります。
   </para>
  </note>
  <note>
   <simpara>
    PHP4.2.1 以降では、この関数は &safemode; と
    <link linkend="ini.open-basedir">open_basedir</link> の影響を受けます。
   </simpara>
  </note>
  <note>
   <para>
    PHP 5.0 以降、この関数は値の中の改行も処理します。
   </para>
  </note>
  <note>
   <simpara>
    ini ファイル上でキーとして使ってはいけない単語があります。
    それらは null, yes, no, true, false などが含まれます。
    null, no および false は "" となり、yes および true は "1" となります。
    次の文字 <literal>{}|&amp;~![()"</literal> は、キーで使ってはいけません。
    また、値の中で特別な意味を持ちます。
   </simpara>
  </note>
  <para>
   初期ファイルの構造は、&php.ini; の構造に似ています。
  </para>
  <para>
   <link linkend="language.constants">定数</link>も ini ファイル上でパースされます。
   そのため、<function>parse_ini_file</function> をコールする前に
   ini ファイル上の値として定数を定義した場合、返り値に統合されます。
   ini ファイル上の値だけが評価されます。以下は例です：
  </para>
  <para>
   <example>
    <title>sample.ini の内容</title>
    <programlisting>
<![CDATA[
; これは設定ファイルのサンプルです。
; php.ini と同様、';' で始まる行はコメントです。

[first_section]
one = 1
five = 5
animal = BIRD

[second_section]
path = "/usr/local/bin"
URL = "http://www.example.com/~username"
]]>
      </programlisting>
     </example>
    </para>
    <para>
     <example>
      <title><function>parse_ini_file</function> の例</title>
      <programlisting role="php">
<![CDATA[
<?php

define('BIRD', 'Dodo bird');

// セクションを無視してパースします。
$ini_array = parse_ini_file("sample.ini");
print_r($ini_array);

// セクションを意識してパースします。
$ini_array = parse_ini_file("sample.ini", true);
print_r($ini_array);

?>
]]>
    </programlisting>
   </example>
  </para>
  <para>
   出力は以下のようになります:
   <informalexample>
    <programlisting>
<![CDATA[
Array
(
    [one] => 1
    [five] => 5
    [animal] => Dodo bird
    [path] => /usr/local/bin
    [URL] => http://www.example.com/~username
)
Array
(
    [first_section] => Array
        (
            [one] => 1
            [five] => 5
            [animal] = Dodo bird
        )

    [second_section] => Array
        (
            [path] => /usr/local/bin
            [URL] => http://www.example.com/~username
        )

)
]]>
    </programlisting>
   </informalexample>
  </para>
  <para>
   数字から始まるキーおよびセクション名は、PHP では
   <link linkend="language.types.integer">整数</link> として評価されます。
   よって、0 で始まる数字は 8 進数として評価され、0x で始まる数字は
   16 進数として評価されます。
  </para>
 </refsect1>
</refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
