<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision: 1.6 $ -->
<!-- splitted from ./ja/functions/strings.xml, last change in rev 1.1 -->
<!-- EN-Revision: 1.12 Maintainer: hirokawa Status: ready -->
<!-- CREDITS: shimooka -->
  <refentry id="function.substr">
   <refnamediv>
    <refname>substr</refname>
    <refpurpose>文字列の一部分を返す</refpurpose>
   </refnamediv>
   <refsect1>
    <title>説明</title>
     <methodsynopsis>
      <type>string</type><methodname>substr</methodname>
      <methodparam><type>string</type><parameter>string</parameter></methodparam>
      <methodparam><type>int</type><parameter>start</parameter></methodparam>
      <methodparam choice="opt"><type>int</type><parameter>length</parameter></methodparam>
     </methodsynopsis>
    <para>
     <function>substr</function> は文字列 <parameter>string
     </parameter> の、<parameter>start</parameter> で指定された
     位置から <parameter>length</parameter> バイト分の文字列を
     返します。
    </para>
    <para>
     <parameter>start</parameter> が正の場合、返される文字列は、
     <parameter>string</parameter> の 0 から数えて
     <parameter>start</parameter>番目から始まる文字列となります。
     例えば、文字列'<literal>abcdef</literal>'において位置
     <literal>0</literal>にある文字は、'<literal>a</literal>'であり、
     位置<literal>2</literal>には'<literal>c</literal>'があります。
    </para>
    <example>
     <title><function>substr</function> の基本的な使用法</title>
     <programlisting role="php">
<![CDATA[
<?php
echo substr('abcdef', 1);     // bcdef を返す
echo substr('abcdef', 1, 3);  // bcd を返す
echo substr('abcdef', 0, 4);  // abcd を返す
echo substr('abcdef', 0, 8);  // abcdef を返す
echo substr('abcdef', -1, 1); // f

// 文字列中の 1 文字にアクセスすることも
// "中括弧" を使用することで可能
$string = 'abcdef';
echo $string{0};                 // a を返す
echo $string{3};                 // d を返す
echo $string{strlen($string)-1}; // f を返す

?>
]]>
     </programlisting>
    </example>
    <para>
     <parameter>start</parameter> が負の場合、返される文字列は、
     <parameter>string</parameter>の後ろから数えて
     <parameter>start</parameter>番目から始まる文字列となります。
    </para>
    <example>
     <title>負の<parameter>start</parameter>を使用</title>
     <programlisting role="php">
<![CDATA[
<?php
$rest = substr("abcdef", -1); //  "f" を返す
$rest = substr("abcdef", -2); // "ef" を返す
$rest = substr("abcdef", -3, 1); // "d" を返す
?>
]]>
     </programlisting>
    </example>
    <para>
     <parameter>length</parameter> が指定され、かつ正である場合、
     返される文字列は <parameter>start</parameter>
     (<parameter>string</parameter> の長さに依存します)
     から数えて<parameter>length</parameter>文字数分となります。
     もし <parameter>string</parameter> が <parameter>start</parameter>
     の文字列長より小さいもしくは等しい場合、&false;
     が返されます。
    </para>
    <para>
     <parameter>length</parameter> が指定され、かつ負である場合、
     <parameter>string</parameter> の終端から多くの文字が省略されます
     (<parameter>start</parameter> が負の場合、
     開始位置は文字列の終端を過ぎているので) 。
     もし <parameter>start</parameter> が切り出し位置を超える場合、
     空文字が返されます。
    </para>
    <example>
     <title>負の<parameter>length</parameter>を使用</title>
     <programlisting role="php">
<![CDATA[
$rest = substr("abcdef", 0, -1);  // "abcde"を返す
$rest = substr("abcdef", 2, -1);  // "cde"を返す
$rest = substr("abcdef", 4, -4);  // ""を返す
$rest = substr("abcdef", -3, -1); // "de"を返す
]]>
     </programlisting>
    </example>
    <para>
     <function>strrchr</function>、
     <function>substr_replace</function>、
     <function>preg_match</function>、
     <function>trim</function>、
     <function>mb_substr</function> および
     <function>wordwrap</function>
     も参照ください。
    </para>
   </refsect1>
  </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
