<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision: 1.7 $ -->
<!-- EN-Revision: 1.8 Maintainer: hirokawa Status: ready -->
<!-- Purpose: compression -->
<!-- Membership: pecl -->

 <reference id="ref.zip">
  <title>Zipファイル関数 (読込のみ)</title>
  <titleabbrev>Zip</titleabbrev>

  <partintro>
   <section id="zip.intro">
    &reftitle.intro;
    <para>
     このモジュールにより、ZIP 圧縮されたアーカイブとその内部のファイルを
     透過的に読み込むことが可能となります。
    </para>
   </section>

   <section id="zip.requirements">
    &reftitle.required;
    <para>
     このモジュールは、Guido Draheim により作成されたライブラリ
     <ulink url="&url.zziplib;">ZZIPlib</ulink> の関数を使用します。
     ZZIPlib バージョン &gt;= 0.10.6 が必要です。
    </para>
    <para>
     PECL パッケージのバージョン 1.1.0 以降では、
     外部パッケージを必要としなくなりました。また、書き込み
     (作成および変更) モードが提供されるようになりました。
     新しい機能はまだドキュメント化されていないので、
     パッケージに含まれる例を参照ください。
    </para>
    <para>
     ZZIPlib は ZIP の圧縮アルゴリズムの完全な実装で提供される関数の
     サブセットであり、ZIP ファイルアーカイブの読込みのみができることに
     注意してください。このライブラリで読み込まれる ZIP ファイルアーカイブを
     作成するには、通常の ZIP ユーティリティが必要です。
    </para>
   </section>

   &reference.zip.configure;

   <section id="zip.configuration">
    &reftitle.runtime;
    &no.config;
   </section>

   <section id="zip.resources">
    &reftitle.resources;
    &no.resource;
   </section>

   <section id="zip.constants">
    &reftitle.constants;
    &no.constants;
   </section>

   <section id="zip.examples">
    &reftitle.examples;
    <para>
     以下の例は、ZIP ファイルアーカイブをオープンし、そのアーカイブの中の
     各ファイルを読み込んで、その内容を出力するものです。この例で使用されている
     <filename>test2.php</filename> アーカイブは、ZZIPlib
     ソース配布に含まれるテスト用のアーカイブの一つです。
    </para>
    <example>
     <title>Zip の使用例</title>
     <programlisting role="php">
<![CDATA[
<?php

$zip = zip_open("/tmp/test2.zip");

if ($zip) {

    while ($zip_entry = zip_read($zip)) {
        echo "Name:               " . zip_entry_name($zip_entry) . "\n";
        echo "Actual Filesize:    " . zip_entry_filesize($zip_entry) . "\n";
        echo "Compressed Size:    " . zip_entry_compressedsize($zip_entry) . "\n";
        echo "Compression Method: " . zip_entry_compressionmethod($zip_entry) . "\n";

        if (zip_entry_open($zip, $zip_entry, "r")) {
            echo "File Contents:\n";
            $buf = zip_entry_read($zip_entry, zip_entry_filesize($zip_entry));
            echo "$buf\n";

            zip_entry_close($zip_entry);
        }
        echo "\n";

    }

    zip_close($zip);

}

?>
]]>
     </programlisting>
    </example>
   </section>
  </partintro>

&reference.zip.functions;

 </reference>
<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
-->

