 <reference id="ref.snmp">
  <title>SNMP関数</title>
  <titleabbrev>SNMP</titleabbrev>

  <partintro>
  <simpara>
  UNIX 上で SNMP 関数を使用するためには、<ulink url="&url.ucd-snmp;">
  UCD SNMP</ulink> パッケージをインストールする必要があります。
  Windows 上では、これらの関数は NT 上でのみ利用可能であり、
  Win95/98 では利用できません。</simpara>
 
    <simpara>
    重要: UCD SNMP パッケージを使用するには、コンパイル前に
     NO_ZEROLENGTH_COMMUNITY を 1 に定義する必要があります。
     UCD SNMP のコンフィグレーションを行った後、config.h を編集し、
     NO_ZEROLENGTH_COMMUNITY を探してください。
     #define の行のコメントを外してください。
     これにより、次のようになるはずです。</simpara>
 
    <para>
     <programlisting>
 #define NO_ZEROLENGTH_COMMUNITY 1
     </programlisting>
    </para>
 
    <simpara>
    SNMP コマンドと組み合わせた場合に奇妙なセグメント例外が生じる場合は、
    上の指示に従っていないと思われます。
    UCD SNMP を再コンパイルしたくない場合は、この不具合に対処するため
    PHP を --enable-ucd-snmp-hack スイッチをつけてコンパイルしてください。</simpara>
     
  </partintro>

  <refentry id="function.snmpget">
   <refnamediv>
    <refname>snmpget</refname>
    <refpurpose>SNMPオブジェクトの取り出し</refpurpose>
   </refnamediv>
   <refsect1>
    <title>解説</title>
    <funcsynopsis>
     <funcdef>string <function>snmpget</function></funcdef>
     <paramdef>string <parameter>hostname</parameter></paramdef>
     <paramdef>string <parameter>community</parameter></paramdef>
     <paramdef>string <parameter>object_id</parameter></paramdef>
     <paramdef>int <parameter><optional>timeout</optional></parameter></paramdef>
     <paramdef>int <parameter><optional>retries</optional></parameter></paramdef>
    </funcsynopsis>

    <para>
    成功したら、SNMPオブジェクトの値を、失敗すればfalseを返します。</para>

    <para>  
    <function>snmpget</function>は<parameter>object_id</parameter>で
	指定されるSNMPオブジェクトの値を
    読みとるために使用されます。
	SNMPエージェントは <parameter>hostname</parameter>で指定され、
    リードコミュニティは、パラメータ<parameter>community</parameter>
	で指定されます。</para>
	<para>
	<informalexample>
      <literallayout>
$syscontact = snmpget("127.0.0.1", "public", "system.SysContact.0")
      </literallayout>
     </informalexample></para>

   </refsect1>
  </refentry>

   <refentry id="function.snmpset">
    <refnamediv>
     <refname>snmpset</refname>
     <refpurpose>SNMP オブジェクトを設定する</refpurpose>
    </refnamediv>
    <refsect1>
     <title>説明</title>
     <funcsynopsis>
      <funcdef>string <function>snmpget</function></funcdef>
      <paramdef>string <parameter>hostname</parameter></paramdef>
      <paramdef>string <parameter>community</parameter></paramdef>
      <paramdef>string <parameter>object_id</parameter></paramdef>
      <paramdef>string <parameter>type</parameter></paramdef>
      <paramdef>mixed <parameter>value</parameter></paramdef>
      <paramdef>int <parameter><optional>timeout</optional></parameter></paramdef>
      <paramdef>int <parameter><optional>retries</optional></parameter></paramdef>
     </funcsynopsis>
 
     <para>
     指定した SNMP オブジェクトの値を設定し、成功時にtrue、エラー時に
     falseを返します。</para>
 
     <para>
      <function>snmpset</function> 関数は、
      <parameter>object_id</parameter> で指定された SNMP オブジェクトの
      値を設定するために使用されます。
      SNMP エージェントを <parameter>hostname</parameter> で指定し、
      リードコミュニティを <parameter>community</parameter> パラメータで
      指定します。</para>
 
    </refsect1>
   </refentry>

  <refentry id="function.snmpwalk">
   <refnamediv>
    <refname>snmpwalk</refname>
    <refpurpose>エージェントから全てのSNMPオブジェクトを取りだす</refpurpose>
   </refnamediv>
   <refsect1>
    <title>解説</title>
    <funcsynopsis>
     <funcdef>array <function>snmpwalk</function></funcdef>
     <paramdef>string <parameter>hostname</parameter></paramdef>
     <paramdef>string <parameter>community</parameter></paramdef>
     <paramdef>string <parameter>object_id</parameter></paramdef>
     <paramdef>int <parameter><optional>timeout</optional></parameter>
     </paramdef>
     <paramdef>int <parameter><optional>retries</optional></parameter>
     </paramdef>
    </funcsynopsis>

    <para>
	
	<function>object_id</function>からルートとしてSNMPオブジェクト
	の値を配列として返します。エラーの場合にFALSEを返します。</para>

    <para>  
    <function>snmpwalk</function>関数は<parameter>hostname</parameter>
	で指定したSNMPエージェントから全ての値を読みとるのに使用されます。
	<parameter>community</parameter>は、そのエージェントに対する
    リードコミュニティを指定します。空の<parameter>object_id</parameter>
	は、SNMPオブジェクトツリーのルートとして解釈され、ツリーの下の全ての
	オブジェクトが配列として返されます。
	 <parameter>object_id</parameter>が指定された場合、
	その <parameter>object_id</parameter>の下の
    全てのSNMPオブジェクトが返されます。

     <informalexample>
      <programlisting role="php"> 
  $a = snmpwalk("127.0.0.1", "public", ""); 
      </programlisting>
     </informalexample></para>

        <para>
        上記の関数コールは、ローカルホスト上で稼働するSNMP
        エージェントから全てのSNMPオブジェクトを返します。
        ループ処理により値を一つずつとりだすことができます。

      <informalexample>
       <programlisting role="php">
 for ($i=0; $i&lt;count($a); $i++) {
      echo $a[$i];
  }
       </programlisting>
      </informalexample></para>
	
   </refsect1>
  </refentry>

 
   <refentry id="function.snmpwalkoid">
    <refnamediv>
     <refname>snmpwalkoid</refname>
     <refpurpose>ネットワークエンティティに関する情報ツリーの検索
     </refpurpose>
    </refnamediv>
    <refsect1>
     <title>説明</title>
     <funcsynopsis>
      <funcdef>array <function>snmpwalkoid</function></funcdef>
      <paramdef>string <parameter>hostname</parameter></paramdef>
      <paramdef>string <parameter>community</parameter></paramdef>
      <paramdef>string <parameter>object_id</parameter></paramdef>
      <paramdef>int <parameter><optional>timeout</optional></parameter>
      </paramdef>
      <paramdef>int <parameter><optional>retries</optional></parameter>
      </paramdef>
     </funcsynopsis>
 
     <para>
     各オブジェクトのIDとその値をルートとして指定した
      <parameter>object_id</parameter> から始まる連想配列として返します。
      エラーの場合はfalseを返します。</para>
 
     <para>
      <function>snmpwalkoid</function> 関数は、hostname で指定した
      SNMP エージェントから全てのオブジェクトIDとその値を読みこむ
      ために使用されます。
      <parameter>community</parameter> は、そのエージェントの
      リードコミュニティを指定します。
      <parameter>object_id</parameter> をnullとした場合は、
      そのSNMPオブジェクトツリーのルートを指定したことになり、
      そのツリー以下の全てのオブジェクトが配列として返されます。
      <parameter>object_id</parameter> が指定された場合、
      <parameter>object_id</parameter> 以下の
      全ての SNMP オブジェクトが返されます。</para>
 
     <para>
      <function>snmpwalkoid</function> および
      <function>snmpwalk</function> があるのは、これまでの経緯
      によります。
      両方の関数は、下位互換のために提供されています。
 
      <informalexample>
       <programlisting role="php"> 
 $a = snmpwalkoid("127.0.0.1", "public", ""); 
       </programlisting>
      </informalexample></para> 
 
     <para>
     上の関数コールは、localhost で実行されている SNMP エージェントから
     全ての SNMP オブジェクトを返します。
     各値は、次のループにより取り出すことができます。
 
      <informalexample>
       <programlisting role="php">
 for (reset($a); $i = key($a); next($a)) {
     echo "$i: $a[$i]&lt;br>\n";
 }
       </programlisting>
      </informalexample></para>
 	
    </refsect1>
   </refentry>
 
 
   <refentry id="function.snmp-get-quick-print">
    <refnamediv>
     <refname>snmp_get_quick_print</refname>
     <refpurpose>UCD ライブラリの quick_print の現在の設定値を取得する
     </refpurpose>
    </refnamediv>
    <refsect1>
     <title>説明</title>
     <funcsynopsis>
      <funcdef>boolean <function>snmp_get_quick_print</function></funcdef>
      <paramdef>void <parameter></parameter></paramdef>
     </funcsynopsis>
 
     <para>
     UCD ライブラリに保持された quick_print の現在の値を返します。
      デフォルトでは、quick_print はオフです。
 
      <informalexample>
       <programlisting role="php"> 
 $quickprint = snmp_get_quick_print();
       </programlisting>
      </informalexample></para>
 
     <para>
     上の関数コールは、quick_print がオフの場合に
      <systemitem>false</systemitem>、quick_print がオンの場合に
      <systemitem>true</systemitem> を返します。</para>
 
     <para>
      <function>snmp_get_quick_print</function> は、UCB SNMP ライブラリを
      使用している場合のみ利用可能です。
      この関数は、Windows SNMP ライブラリを使用している場合は利用できません。</para>
 
     <para>
      参照: <function>snmp_set_quick_print</function> に quick_print の
      役割に関する詳細な説明があります。</para>
 	
    </refsect1>
   </refentry>
 
   <refentry id="function.snmp-set-quick-print">
    <refnamediv>
     <refname>snmp_set_quick_print</refname>
     <refpurpose>UCB SNMP ライブラリで quick_print の値を設定する
     </refpurpose>
    </refnamediv>
    <refsect1>
     <title>説明</title>
     <funcsynopsis>
      <funcdef>void <function>snmp_set_quick_print</function></funcdef>
      <paramdef>boolean <parameter>quick_print</parameter></paramdef>
     </funcsynopsis>
 
     <para>
     UCB SNMP ライブラリで quick_print の値を設定します。この値を
     (1) に設定した場合、SNMP ライブラリは、'簡潔に表示された
     (quick printed)' 値を返します。
     これは、値のみが出力されることを意味します。
     quick_print が有効でない場合(デフォルト)、UCD SNMP ライブラリは、
     (IPアドレスまたは OIDのような)その値の種類を含むより詳細な情報を
     出力します。
     加えて、quick_print が有効でない場合、ライブラリは3文字以下の全ての
     文字列について16進数値も出力します。</para>
 
     <para>
     quick_print の設定は、返される情報を吟味してから表示する場合に
     使用されることが多いです。
 
      <informalexample>
       <programlisting role="php"> 
 snmp_set_quick_print(0);
 $a = snmpget("127.0.0.1", "public", ".1.3.6.1.2.1.2.2.1.9.1");
 echo "$a&lt;BR>\n";
 snmp_set_quick_print(1);
 $a = snmpget("127.0.0.1", "public", ".1.3.6.1.2.1.2.2.1.9.1");
 echo "$a&lt;BR>\n";
       </programlisting>
      </informalexample></para>
 
     <para>
     最初に出力される値は、'Timeticks: (0) 0:00:00.00' のようになります。
     一方、quick_print を有効にした後は、0:00:00.00' が出力されます。</para>
 
     <para>
     デフォルトで、UCD SNMP ライブラリは冗長な値を返します。
     quick_print は、値のみを返すために用いられます。</para>
 
     <para>
     現在、文字列は引用符で括られて返されますが、この動作は将来の
     リリースでは修正される予定です。</para>
 
     <para>
      <function>snmp_set_quick_print</function> は、UCD SNMP ライブラリ
      を使用している場合のみ利用可能です。
      この関数は、Windows SNMP ライブラリを使用している場合には
      利用できません。</para>
 
    </refsect1>
   </refentry>

 </reference>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
sgml-parent-document:nil
sgml-default-dtd-file:"../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
-->
