<?xml version="1.0" encoding="utf-8"?>
<!-- splitted from ./ja/functions/mysql.xml, last change in rev 1.1 -->
<!-- last change to 'mysql-connect' in en/ tree in rev 1.2 -->
<refentry id="function.mysql-connect">
 <refnamediv>
  <refname>mysql_connect</refname>
  <refpurpose>MySQLサーバーへの接続をオープンする</refpurpose>
 </refnamediv>
 <refsect1>
  <title>説明</title>
  <methodsynopsis>
   <type>resource</type><methodname>mysql_connect</methodname>
   <methodparam choice="opt"><type>string</type><parameter>
     server
    </parameter></methodparam>
   <methodparam choice="opt"><type>string</type><parameter>
     username
    </parameter></methodparam>
   <methodparam choice="opt"><type>string</type><parameter>
     password
    </parameter></methodparam>
   <methodparam choice="opt"><type>bool</type><parameter>
     new_link
    </parameter></methodparam>
   <methodparam choice="opt"><type>int</type><parameter>
     client_flags
    </parameter></methodparam>
  </methodsynopsis>
  <para> 
   接続に成功した場合は正のMySQLリンクID、失敗した場合に&false;を返し
   ます。
  </para>
  <para> 
   <function>mysql_connect</function>は、MySQL サーバーへの接続を確
   立します。オプションのパラメータが指定されない場合、以下のデフォ
   ルト値が使用されます。: 
   <parameter>server</parameter> = 'localhost:3306', 
   <parameter>username</parameter> = サーバプロセスの所有ユーザ名
   <parameter>password</parameter> = 空のパスワード
  </para>
  <para>
   パラメータ<parameter>server</parameter>には、ポート番号も指定する
   ことが可能です。例えば、&quot;hostname:port&quot;、または、
   localhostの場合には、ソケットへのパス
   &quot;:/path/to/socket&quot;とします。
   <note>
    <para>
     Whenever you specify &quot;localhost&quot; or
     &quot;localhost:port&quot; as server, the MySQL client library will
     override this and try to connect to a local socket (named pipe on
     Windows). If you want to use TCP/IP, use &quot;127.0.0.1&quot;
     instead of &quot;localhost&quot;. If the MySQL client library tries to
     connect to the wrong local socket, you should set the correct path as
     mysql.default_host in your PHP configuration and leave the server field
     blank.
    </para>
    <para>
     &quot;:port&quot;のサポートは3.0B4で追加されました。
    </para>
    <para>
     &quot;:/path/to/socket&quot;のサポートは 3.0.10で追加されました。
    </para>
    <para>
     関数名の前に<link
     linkend="language.operators.errorcontrol">@</link>を付けることに
     接続に失敗した場合のエラーメッセージを出力しないようにできます。
    </para>
   </note>
  </para>
  <para> 
   同じ引数で2回<function>mysql_connect</function>をコールした場合、
   二回目は新規のリンクが確立されるのではなく、代わりにすでにオープンされた
   リンクのリンクIDが返されます。
   パラメータ<parameter>new_link</parameter>はこの動作を変更し、既に
   <function>mysql_connect</function>が同じパラメータでコールされてい
   る場合でも常に新規のリンクがオープンされるようにします。
   パラメータ<parameter>client_flags</parameter>は、
   定数   MYSQL_CLIENT_COMPRESS, MYSQL_CLIENT_IGNORE_SPACE,
   MYSQL_CLIENT_INTERACTIVEの組み合わせとすることができます。
  </para>
  <note>
   <para>
    パラメータ<parameter>new_link</parameter>はPHP 4.2.0で利用可能とな
    りました。
   </para>
   <para>
    パラメータ<parameter>client_flags</parameter>はPHP 4.3.0で利用可能
    となりました。
   </para>
  </note>
  <para> 
   サーバーへのリンクは、<function>mysql_close</function>のコールにより
   明示的に閉じられない限り、スクリプトの実行終了と同時に閉じられます。
  </para>
  <example>
   <title>MySQL に接続する例</title>
   <programlisting role="php">
<![CDATA[
<?php
    $link = mysql_connect("localhost", "username", "secret")
        or die("Could not connect");
    print ("Connected successfully");
    mysql_close($link);
?>
]]>
   </programlisting>
  </example>
  <para> 
   <function>mysql_pconnect</function>および
   <function>mysql_close</function>も参照下さい。
  </para>
 </refsect1>
</refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
