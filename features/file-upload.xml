<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision: 1.24 $ -->
<!-- EN-Revision: 1.80 Maintainer: hirokawa Status: ready -->
 <chapter id="features.file-upload">
  <title>ファイルアップロードの処理</title>
  
  <sect1 id="features.file-upload.post-method">
   <title>POST メソッドによるアップロード</title>
   <simpara>
    この機能により、テキスト、バイナリファイルの両方をアップロードできるように
    なります。
    PHPの認証機構およびファイル操作関数を用いて、アップロードを許可する
    ユーザーとアップロード後にそのファイルを使用して行う動作を完全に制御する
    ことが可能です。
   </simpara>
   <simpara>
    PHP は、全てのRFC-1867対応ブラウザ(Netscape Navigator 3 以上、
    MicrosoftからのパッチをあてたMicrosoft Internet Explorer 3または
    パッチ無しのそれ以降の版を含みます)からファイルのアップロードを
    受けることができます。
   </simpara>

   <note>
    <title>関係する設定に関する注記</title>
    <para>
     &php.ini;の
     <link linkend="ini.file-uploads">file_uploads</link>, 
     <link linkend="ini.upload-max-filesize">upload_max_filesize</link>,
     <link linkend="ini.upload-tmp-dir">upload_tmp_dir</link>,
     <link linkend="ini.post-max-size">post_max_size</link>,
     <link linkend="ini.max-input-time">max_input_time</link>
     ディレクティブも参照ください。
    </para>
   </note>

   <para>
    PHPは　<productname>Netscape Composer</productname>　および
    W3Cの　<productname>Amaya</productname>クライアントにより使用される
    PUTメソッドによるファイルアップロードもサポートしています。詳細は、<link
    linkend="features.file-upload.put-method">PUTメソッドのサポート
    </link>を参照ください。
   </para>

   <para>
    <example>
     <title>ファイルアップロード用のフォーム</title>
     <para>
      ファイルアップロード画面は、次のような特別なフォームを作成すること
      により、作成することができます。
      </para>
     <programlisting role="html">
     <!-- The HTML comments in this example code are stripped.
     This needs to be fixed in livedocs. -->
<![CDATA[
<!-- The data encoding type, enctype, MUST be specified as below -->
<form enctype="multipart/form-data" action="__URL__" method="POST">
    <!-- MAX_FILE_SIZE must precede the file input field -->
    <input type="hidden" name="MAX_FILE_SIZE" value="30000" />
    <!-- Name of input element determines name in $_FILES array -->
    Send this file: <input name="userfile" type="file" />
    <input type="submit" value="Send File" />
</form>
]]>
     </programlisting>
     <para>
      上の例の<literal>__URL__</literal>は、PHPファイルを指すよう置換される
      必要があります。
     </para>
     <para>
      hidden フィールド<literal>MAX_FILE_SIZE</literal>は、
      input フィールド file の前に置く必要があります。
      この値は、取得可能なファイルの最大サイズを規定します。この値はバイト数で指定します。
      これはブラウザへの勧告に過ぎず、PHPはこれも確認します。
      ブラウザ側でこの最大値を出し抜くのは簡単なことなので、この機能を
      信頼して、これより大きなサイズのファイルがブロックされることを前提にしては
      いけません。
      しかし、PHP側の最大サイズの設定を欺くことはできません。
      しかしそれでも<literal>MAX_FILE_SIZE</literal>を指定すべきです。なぜなら、
      巨大なファイルを転送しようとして、実はそれが大きすぎて
      転送できないということを長時間待ったあとで知らされるのを
      防げるからです。
     </para>
     <para>
      アップロード用のフォームが
      <literal>enctype="multipart/form-data"</literal>属性を有しているかを
      確認してください。さもないと、ファイルのアップロードは動作しません。
     </para>
    </example>
   </para>

   <para>
    オートグローバル<link linkend="reserved.variables.files">$_FILES</link>
    は、PHP 4.1.0以降に存在します。
    (<varname>$HTTP_POST_FILES</varname>は、これ以前のバージョンに存在します。)
    これらの配列には、全てアップロードされたファイルの情報が
    含まれています。
   </para>

   <para>
    <link linkend="reserved.variables.files">$_FILES</link>
    の内容は次のようになります。ここでは、上の例のスクリプトで使われたように、
    アップロードファイルの名前として<emphasis>userfile</emphasis>を使用する
    ことを仮定していることに注意してください。
    実際にはどんな名前にすることもできます。
    <variablelist>
     <varlistentry>
      <term><varname>$_FILES['userfile']['name']</varname></term>
      <listitem>
       <para>
        クライアントマシンの元のファイル名。
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term><varname>$_FILES['userfile']['type']</varname></term>
      <listitem>
       <para>
        ファイルのMIME型。ただし、ブラウザがこの情報を提供する場合。
        例えば、<literal>"image/gif"</literal>のようになります。
        </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term><varname>$_FILES['userfile']['size']</varname></term>
      <listitem>
       <para>
        アップロードされたファイルのバイト単位のサイズ。
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term><varname>$_FILES['userfile']['tmp_name']</varname></term>
      <listitem>
       <para>
        アップロードされたファイルがサーバー上で保存されているテンポラ
        リファイルの名前。
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term><varname>$_FILES['userfile']['error']</varname></term>
      <listitem>
       <para>
        このファイルアップロードに関する
        <link linkend="features.file-upload.errors">エラーコード</link>
        <emphasis>['error']</emphasis>は、PHP 4.2.0で追加されました。
       </para>
      </listitem>
     </varlistentry>
    </variablelist>
   </para>

   <para>
    &php.ini; の <link
    linkend="ini.upload-tmp-dir">upload_tmp_dir</link>ディレクティブで
    他の場所を指定しない限り、ファイルはサーバーにおけるデフォルトのテ
    ンポラリディレクトリに保存されます。サーバーのデフォルトディレクト
    リは、PHP を実行する環境において環境変数 <envar>TMPDIR</envar>を設
    定することにより変更することができます。しかし、PHP スクリプトの内
    部から<function>putenv</function> 関数により設定しても上手くいきま
    せん。この環境変数は、アップロードされたファイルに他の処理を行う際
    にも同様に使用することが可能です。
    <example>
     <title>ファイルのアップロードを検証する</title>
     <para>
      詳細については、<function>is_uploaded_file</function>
      および<function>move_uploaded_file</function>の関数のエントリも
      参照ください。以下はフォームからファイルをアップロードするプロセスの例です。
     </para>
     <programlisting role="php">
<![CDATA[
<?php
// 4.1.0より前のPHPでは$FILESの代わりに$HTTP_POST_FILESを使用する必要
// があります。

$uploaddir = '/var/www/uploads/';
$uploadfile = $uploaddir . basename($_FILES['userfile']['name']);

echo '<pre>';
if (move_uploaded_file($_FILES['userfile']['tmp_name'], $uploadfile)) {
    echo "File is valid, and was successfully uploaded.\n";
} else {
    echo "Possible file upload attack!\n";
}

echo 'Here is some more debugging info:';
print_r($_FILES);

print "</pre>";

?>
]]>
     </programlisting>
    </example>
   </para>
   <simpara>
    アップロードされたファイルを受け取る PHP スクリプトは、アップロー
    ドされたファイルを用いて何をするべきかを決めるために必要なロジック
    を全て実装する必要があります。例えば、変数
    <varname>$_FILES['userfile']['size']</varname>を使用して、小さすぎ
    たり、大きすぎたりするファイルを捨てることができます。指定した型以
    外のファイルを全て捨てるために変数
    <varname>$_FILES['userfile']['type']</varname>を用いることができま
    す。
    PHP 4.2.0以降、
    <varname>$_FILES['userfile']['error']</varname> を使用することができ、
    <link linkend="features.file-upload.errors">エラーコード</link>
    に基づき、ロジックを構成することができます。
    何らかの方法により、テンポラリディレクトリからファイルを削除し
    たり、他の場所に移動したりする必要があります。
   </simpara>
   <simpara>
    フォームでアップロードされるファイルが選択されていない場合、PHPは、
    <varname>$_FILES['userfile']['size']</varname> として 0を返し、
    <varname>$_FILES['userfile']['tmp_name']</varname> には値を
    設定しません。
   </simpara>
   <simpara>
    移動または名前の変更が行われていない場合、リクエストの終了時にその
    ファイルはテンポラリディレクトリから削除されます。
   </simpara>
  </sect1>
 
  <sect1 id="features.file-upload.errors">
   <title>エラーメッセージの説明</title>
   <simpara>
    PHP 4.2.0以降、PHPはファイル配列とともに適当なエラーコードを返します。
    エラーコードは、PHPによるファイルアップロードの間に生成され、
    ファイル配列の<emphasis>['error']</emphasis>要素で
    アクセス可能です。言い換えると、エラーは、
    <varname>$_FILES['userfile']['error']</varname>でアクセス可能でしょう。
   </simpara>
   <para>
    <variablelist>
     <varlistentry>
      <term><varname>UPLOAD_ERR_OK</varname></term>
      <listitem>
       <para>
        値: 0; エラーはなく、ファイルアップロードは成功しています。
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term><varname>UPLOAD_ERR_INI_SIZE</varname></term>
      <listitem>
       <para>
        値: 1; アップロードされたファイルは、&php.ini; の
        <link linkend="ini.upload-max-filesize">upload_max_filesize</link> 
        ディレクティブの値を超えています。
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term><varname>UPLOAD_ERR_FORM_SIZE</varname></term>
      <listitem>
       <para>
        値: 2; アップロードされたファイルは、HTMLフォームで指定された
        <emphasis>MAX_FILE_SIZE</emphasis> を超えています。
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term><varname>UPLOAD_ERR_PARTIAL</varname></term>
      <listitem>
       <para>
        値: 3; アップロードされたファイルは一部のみしかアップロードされていません。
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term><varname>UPLOAD_ERR_NO_FILE</varname></term>
      <listitem>
       <para>
        値: 4; ファイルはアップロードされませんでした。
       </para>
      </listitem>
     </varlistentry>
    </variablelist>
   </para>
   <note>
    <para>
     これらはPHP 4.3.0でPHP定数となりました。
    </para>
   </note>
  </sect1>

  <sect1 id="features.file-upload.common-pitfalls">
   <title>陥りやすい落とし穴</title>
   <simpara>
    <literal>MAX_FILE_SIZE</literal>に<link
    linkend="ini.upload-max-filesize">upload_max_filesize</link>
    で指定されたファイルサイズより大きなファイルサイズを指定する
    ことはできません。デフォルトは、2メガバイトです。
   </simpara>
   <simpara>
    メモリ制限が有効な場合、<link
    linkend="ini.memory-limit">memory_limit</link>により大きな値を設定
    することが必要となる可能性があります。
    <link linkend="ini.memory-limit">memory_limit</link>に充分大きな値
    を設定するようにしてください。
   </simpara>
   <simpara>
    <link linkend="ini.max-execution-time">max_execution_time</link>
    に設定した値が小さすぎた場合、スクリプトの実行時間がこの値を越える
    可能性を生じます。
    <literal>max_execution_time</literal>に充分大きな値を設定するよう
    にしてください。
   </simpara>
   <note>
    <simpara>
     <link linkend="ini.max-execution-time">max_execution_time</link>は
     スクリプト自身の実行時間にのみ影響します。
     スクリプトの実行範囲の外側で発生する動作にかかる時間、つまり、
     <function>system</function>を使ったシステムコールや、
     <function>sleep</function>関数、データベースに対するクエリー、
     ファイルアップロードプロセス、などに費やされた時間はスクリプトの
     総実行時間に含まれません。
    </simpara>
   </note>    
   <simpara>
    <link linkend="ini.post-max-size">post_max_size</link>の設定値が小
    さすぎた場合、大きなファイルをアップロードすることができなくなりま
    す。<literal>post_max_size</literal>に充分大きな値を設定するように
    してください。
   </simpara>
   <simpara>
    処理するファイルを検証しない場合、ユーザーが他のディレクトリにある
    非公開情報にアクセスできる可能性を生じます。
   </simpara>
   <simpara>
    <productname>CERN httpd</productname>は、クライアントから得た 
    content-type MIME ヘッダにおいて最初が空白文字で始まるものを切り捨てるようですので注意してください。
    このような動作をする限り、<productname>CERN httpd</productname>は、
    ファイルアップロード機能をサポートしないでしょう。
   </simpara>
   <simpara>
    大量のディレクトリ一覧のスタイルのせいで、
    風変わりな名前（空白を含んでいるとか）のファイルを適切に扱えることは
    保証できません。
   </simpara>
   <simpara>
    A developer may not mix normal input fields and file upload fields in the same
    form variable (by using an input name like <literal>foo[]</literal>).
   </simpara>
  </sect1>

  <sect1 id="feature-fileupload.multiple">
   <title>複数ファイルのアップロード</title>
   <simpara>
    <literal>input</literal>で異なった<literal>name</literal>を使用す
    ることにより、複数のファイルをアップロードすることができます。
   </simpara>
   <simpara>
    複数のファイルを一度にアップロードし、自動的にまとめられた情報を
    配列で取得することが可能です。これを行うには、HTMLフォームで複数選
    択可能なセレクトやチェックボックスを指定する際と同様に配列を用いた
    投稿用の構文を使用する必要があります。
   </simpara>
   <note>
    <para>
     複数ファイルのアップロード機能は 3.0.10以降サポートされました。
    </para>
   </note>
   <para>
    <example>
     <title>複数ファイルのアップロード</title>
     <programlisting role="html">
<![CDATA[
<form action="file-upload.php" method="post" enctype="multipart/form-data">
  Send these files:<br />
  <input name="userfile[]" type="file" /><br />
  <input name="userfile[]" type="file" /><br />
  <input type="submit" value="Send files" />
</form>
]]>
     </programlisting>
    </example>
   </para>
   <simpara>
    上記のフォームで投稿された場合、配列
    <varname>$_FILES['userfile']</varname>,
    <varname>$_FILES['userfile']['name']</varname>, 
    <varname>$_FILES['userfile']['size']</varname>
    (4.1.0より前のバージョンのPHPの場合は$HTTP_POST_FILES)が設定されま
    す。
    <literal>register_globals</literal>がonの場合、アップロードファイ
    ルに関してグローバル変数も設定されます。
    これらの各々は、投稿されたファイ
    ルに関する適当な値を有する数値を添字とする配列となります。
   </simpara>
   <simpara>
    例えば、ファイル名が<filename>/home/test/review.html</filename> お
    よび<filename>/home/test/xwp.out</filename>のファイルが投稿された
    としましょう。この場合、
    <varname>$_FILES['userfile']['name'][0]</varname>の値が
    <filename>review.html</filename>となり、
    <varname>$_FILES['userfile']['name'][1]</varname>の値が
    <filename>xwp.out</filename>となります。
    同様に、<varname>$_FILES['userfile']['size'][0]</varname>の値が
    <filename>review.html</filename>のファイルサイズといったようになり
    ます。
   </simpara>
   <simpara>
    <varname>$_FILES['userfile']['name'][0]</varname>,
    <varname>$_FILES['userfile']['tmp_name'][0]</varname>,
    <varname>$_FILES['userfile']['size'][0]</varname>, 
    <varname>$_FILES['userfile']['type'][0]</varname>も設定されます。
   </simpara>
  </sect1>

  <sect1 id="features.file-upload.put-method">
   <title>PUT メソッドのサポート</title>
   <simpara>
    PUT method support has changed between PHP 3 and PHP 4.
    In PHP 4, one should use the standard input stream to read
    the contents of an HTTP PUT.
    PHP3とPHP4の間でPUTメソッドのサポートが変更されました。
    PHP4では、HTTP PUTのコンテンツを読み込むためには
    標準の入力ストリームを使用すべきです。
   </simpara>
   <para>
    <example>
     <title>PHP 4でHTTP PUTファイルを保存する</title>
     <programlisting role="php">
<![CDATA[
<?php
/* PUT データが標準入力ストリームから入ってくる */
$putdata = fopen("php://stdin","r");

/* 書き込み先ファイルをオープン */
$fp = fopen("myputfile.ext","w");

/* 1kb毎に読み込んでファイルに書き込む */
while ($data = fread($putdata,1024))
  fwrite($fp,$data);

/* ストリームを閉じる */
fclose($fp);
fclose($putdata);
?>
]]>
     </programlisting>
    </example>
   </para>
   <note>
    <para>
     以下の全てのドキュメントはPHP3にのみ適用されます。
    </para>
   </note>
   <para>
    PHPは、<productname>Netscape Composer</productname>や 
    W3C <productname>Amaya</productname>のようなクライアントを使用した場合の
    HTTP PUTメソッドのサポートを提供します。
    PUTリクエストはファイルアップロードよりもシンプルであり、
    次のような形になります。
    <informalexample>
     <programlisting>
PUT /path/filename.html HTTP/1.1
     </programlisting>
    </informalexample>
   </para>
   <para>
    これは、通常、リモートクライアントが <filename>/path/filename.html</filename>
    が指す内容を Web ツリーに保存したいことを意味します。 Apache または PHP に
    おいて全ての人がWebツリー上の任意のファイルを自動的に上書きできる
    ようにするというのは明らかに良い発想ではありません。よって、このよ
    うなりクエストを処理する際には、まずそのリクエストを処理する特定の
    PHPスクリプトが必要なことをwebサーバーに通知する必要があります。
    Apacheにおいては、<emphasis>Script</emphasis> ディレクティブにより
    これを行うことができます。これは、Apache設定ファイルのほぼ任意の場
    所に置くことができます。一般的なのは、&lt;Directory&gt; ブロックま
    たは &lt;Virtualhost&gt; ブロックの中です。次のように指定します。
    <informalexample>
     <programlisting>
Script PUT /put.php
    </programlisting>
    </informalexample>
   </para>
   <simpara>
    これにより、この行を指定したコンテキストにマッチするURIを有する全
    てのPUTリクエストが put.php スクリプトに送られるよう Apache に指
    定します。 もちろん、拡張子 .php により PHP が実行されるよう設定
    され、PHP がアクティブであることが必要です。
   </simpara>
   <simpara>
    put.php ファイルの中で次のように処理を行う必要があります。
   </simpara>
   <para>
    <informalexample>
     <programlisting role="php">
<![CDATA[
<?php copy($PHP_UPLOADED_FILE_NAME,$DOCUMENT_ROOT.$REQUEST_URI); ?>
]]>
    </programlisting>
    </informalexample>
   </para>
   <simpara>
    これは、リモートクライアントによりリクエストされた位置にファイルを
    コピーします。このファイルコピーを実行する前に何らかの確認やユーザー
    の認証を実行することが望ましいでしょう。上記のスクリプトでは、PHP
    がPUTメソッドのリクエストを受けた際に <link
    linkend="features.file-upload.post-method">POSTメソッド</link>
    と全く同様にテンポラリファイルにアップロードされたファイルを保存す
    るということを利用しています。リクエストが終了した際に、テンポラリ
    ファイルは削除されます。よって、PUTを処理するPHPスクリプトは、この
    ファイルをどこかにコピーする必要があります。このテンポラリファイル
    のファイル名は <varname>$PHP_PUT_FILENAME</varname>変数に保持されており、
    <varname>$REQUEST_URI</varname>(Apache以外のWebサーバーでは変わる可能性があります)
    で指定された送信先ファイル名を得ることができます。送信先ファイル名
    は、リモートクライアントが指定したものです。このクライアントの指定
    を必ずしも受ける必要はありません。例えば、アップロードされた全ての
    ファイルを特別なアップロード用ディレクトリにコピーすることも可能で
    す。
   </simpara>
  </sect1>

 </chapter>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
-->
