<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision: 1.2 $ -->
<!-- EN-Revision: 1.5 Maintainer: hirokawa Status: ready -->
 <refentry id="function.imagecolorallocatealpha">
  <refnamediv>
   <refname>imagecolorallocatealpha</refname>
   <refpurpose>画像で使用する色を透過度を指定して作成する</refpurpose>
  </refnamediv>
  <refsect1>
   <title>説明</title>
    <methodsynopsis>
     <type>int</type><methodname>imagecolorallocatealpha</methodname>
     <methodparam><type>resource</type><parameter>image</parameter></methodparam>
     <methodparam><type>int</type><parameter>red</parameter></methodparam>
     <methodparam><type>int</type><parameter>green</parameter></methodparam>
     <methodparam><type>int</type><parameter>blue</parameter></methodparam>
     <methodparam><type>int</type><parameter>alpha</parameter></methodparam>
    </methodsynopsis>
   <para>
    <function>imagecolorallocatealpha</function>は、透明度を指定する
    パラメータが追加されている以外は<function>imagecolorallocate</function>
    と等価です。透明度は<literal>0</literal>から<literal>127</literal>
    までの値でセットします。<literal>0</literal>は完全に不透明で
    <literal>127</literal>は完全に透明となります。
   </para>
   <para>
    作成に失敗すると&false;を返します。
   </para>
   <para>
    <example>
     <title><function>imagecolorallocatealpha</function>の使用例</title>
     <programlisting role="php">
<![CDATA[
<?php
$size = 300;
$image=imagecreatetruecolor($size, $size);

// 白い背景で黒いふちどりにする
$back = imagecolorallocate($image, 255, 255, 255);
$border = imagecolorallocate($image, 0, 0, 0);
imagefilledrectangle($image, 0, 0, $size - 1, $size - 1, $back);
imagerectangle($image, 0, 0, $size - 1, $size - 1, $border);

$yellow_x = 100;
$yellow_y = 75;
$red_x    = 120;
$red_y    = 165; 
$blue_x   = 187;
$blue_y   = 125; 
$radius   = 150;

// alpha値を指定して色を作成する
$yellow = imagecolorallocatealpha($image, 255, 255, 0, 75);
$red    = imagecolorallocatealpha($image, 255, 0, 0, 75);
$blue   = imagecolorallocatealpha($image, 0, 0, 255, 75);

// 3つの重なる円を描く
imagefilledellipse($image, $yellow_x, $yellow_y, $radius, $radius, $yellow);
imagefilledellipse($image, $red_x, $red_y, $radius, $radius, $red);   
imagefilledellipse($image, $blue_x, $blue_y, $radius, $radius, $blue);

// 正しいヘッダを出力するのを忘れずに!
header('Content-type: image/png');

// 最後に、結果を出力します
imagepng($image);
imagedestroy($image);
?>
]]>
     </programlisting>
    </example>
   </para>
   <para>
    <function>imagecolorallocate</function>と
    <function>imagecolordeallocate</function>も参照してください。
   </para>
  </refsect1>
 </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
