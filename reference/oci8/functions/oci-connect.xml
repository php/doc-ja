<?xml version="1.0" encoding="utf-8" ?>
<!-- $Revision: 1.2 $ -->
<!-- EN-Revision: 1.8 Maintainer: shimooka Status: ready -->
  <refentry id="function.oci-connect">
   <refnamediv>
    <refname>oci_connect</refname>
    <refpurpose>Oracle サーバへの接続を確立する</refpurpose>
   </refnamediv>
   <refsect1>
    <title>説明</title>
     <methodsynopsis>
      <type>resource</type><methodname>oci_connect</methodname>
      <methodparam><type>string</type><parameter>username</parameter></methodparam>
      <methodparam><type>string</type><parameter>password</parameter></methodparam>
      <methodparam choice="opt"><type>string</type><parameter>db</parameter></methodparam>
      <methodparam choice="opt"><type>string</type><parameter>charset</parameter></methodparam>
      <methodparam choice="opt"><type>int</type><parameter>session_mode</parameter></methodparam>
     </methodsynopsis>
    <para>
     <function>oci_connect</function> は他のほとんどの OCI コールで必要な
     接続 ID を返します。
     オプションの3番目のパラメータには、ローカルOracleインスタンスの名前か
     <filename>tnsnames.ora</filename>
     における接続先のエントリ名を指定することができます。
     オプションの3番目のパラメータが指定されない場合、
     PHP は接続先のデータベースを決定するために環境変数
     <constant>ORACLE_SID</constant> (Oracle
     インスタンス)または <constant>TWO_TASK</constant>
     (<filename>tnsnames.ora</filename>) を使用します。
    </para>
    <para>
     パラメータ <parameter>session_mode</parameter> はバージョン
     1.1 からり用可能で、次の値を受け付けます:
     <constant>OCI_DEFAULT</constant>,
     <constant>OCI_SYSOPER</constant>, <constant>OCI_SYSDBA</constant> 。
     <constant>OCI_SYSOPER</constant> もしくは
     <constant>OCI_SYSDBA</constant> のいずれかが指定された場合、
     <function>oci_connect</function> は外部の信用を利用して
     権限付きの接続を確立しようとします。
     デフォルトでは権限付きの接続は無効です。有効にするためには、<link
      linkend="ini.oci8.privileged_connect">oci8.privileged_connect</link>
     をオンにしてください。
    </para>
    <note>
     <simpara>
      もし PHP を Oracle Instant Client と併用している場合、
      次に解説されている簡単な接続ネーミングメソッドを使用することができます:
      <ulink url="&url.oracle.oic.connect;">&url.oracle.oic.connect;</ulink> 。
      基本的にデータベース名として "//db_host[:port]/database_name"
      を指定できることを意味します。しかし、
      古いネーミング方法を使用したい場合、<constant>ORACLE_HOME</constant>
      もしくは <constant>TNS_ADMIN</constant> のいずれかを設定
      <emphasis>しなければなりません</emphasis>。
     </simpara>
    </note>
    <note>
     <simpara>
      同じパラメータを使用して 2 回目やそれ以降に
      <function>oci_connect</function> がコールされた場合、
      最初のコールで返された接続ハンドルを返します。
      これは 1 つのハンドルに対して発行されたクエリは、
      他のハンドルにも適用されることを意味します。なぜなら、
      これらは <emphasis>同じ</emphasis> ハンドルだからです。
      この動作は以下の例 1 で例示されています。
      もしトランザクション的にお互い独立した 2 つのハンドルが必要な場合、
      <function>oci_new_connect</function> を使用してください。
     </simpara>
    </note>
    &oci.charset;
    <para>
     <example>
      <title><function>oci_connect</function> の例</title>
      <programlisting role="php">
<![CDATA[
<?php
echo "<pre>";
$db = "";

$c1 = oci_connect("scott", "tiger", $db);
$c2 = oci_connect("scott", "tiger", $db);

function create_table($conn)
{
  $stmt = oci_parse($conn, "create table scott.hallo (test varchar2(64))");
  oci_execute($stmt);
  echo $conn . " created table\n\n";
}

function drop_table($conn)
{
  $stmt = oci_parse($conn, "drop table scott.hallo");
  oci_execute($stmt);
  echo $conn . " dropped table\n\n";
}

function insert_data($conn)
{
  $stmt = oci_parse($conn, "insert into scott.hallo
            values('$conn' || ' ' || to_char(sysdate,'DD-MON-YY HH24:MI:SS'))");
  oci_execute($stmt, OCI_DEFAULT);
  echo $conn . " inserted hallo\n\n";
}

function delete_data($conn)
{
  $stmt = oci_parse($conn, "delete from scott.hallo");
  oci_execute($stmt, OCI_DEFAULT);
  echo $conn . " deleted hallo\n\n";
}

function commit($conn)
{
  oci_commit($conn);
  echo $conn . " committed\n\n";
}

function rollback($conn)
{
  oci_rollback($conn);
  echo $conn . " rollback\n\n";
}

function select_data($conn)
{
  $stmt = oci_parse($conn, "select * from scott.hallo");
  oci_execute($stmt, OCI_DEFAULT);
  echo $conn."----selecting\n\n";
  while (oci_fetch($stmt)) {
    echo $conn . " [" . oci_result($stmt, "TEST") . "]\n\n";
  }
  echo $conn . "----done\n\n";
}

create_table($c1);
insert_data($c1);   // c1 を使って行を挿入
insert_data($c2);   // c2 を使って行を挿入

select_data($c1);   // 両方の挿入した結果が返される
select_data($c2);

rollback($c1);      // c1 を使ってロールバック

select_data($c1);   // どちらの挿入もロールバックされている
select_data($c2);

insert_data($c2);   // c2 を使って行を挿入
commit($c2);        // c2 を使ってコミット

select_data($c1);   // c2 の結果が返される

delete_data($c1);   // c1 を使ってテーブル内の全ての行を削除
select_data($c1);   // 行は返されない
select_data($c2);   // 行は返されない
commit($c1);        // c1 を使ってコミット

select_data($c1);   // 行は返されない
select_data($c2);   // 行は返されない

drop_table($c1);
echo "</pre>";
?>
]]>
      </programlisting>
     </example>
    </para>
    <para>
     エラーの場合、<function>oci_connect</function> は &false; を返します。
    </para>
    <note>
     <para>
      PHP バージョン 5.0.0 以前では、代わりに
      <function>ocilogon</function>
      を使用しなければなりません。
      まだこの名前を使用することができ、下位互換性のため
      <function>oci_connect</function> への別名として残されていますが、
      推奨されません。
     </para>
    </note>
    <simpara>
     <function>oci_pconnect</function>,
     <function>oci_new_connect</function>,
     <function>oci_close</function> も参照ください。
    </simpara>
   </refsect1>
  </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
