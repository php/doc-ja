<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision: 1.4 $ -->
<!-- sync: 1.3 -->
<sect1 id="install.openbsd">
 <title>Unix/OpenBSD インストール</title>
 <para>
  本節では、PHPを <ulink url="&url.openbsd;">OpenBSD</ulink> にイン
  ストールする場合に固有の注意事項とヒントについて説明します。
 </para>
 <sect2 id="install.openbsd.packages">
  <title>バイナリパッケージの使用</title>
   <simpara>
    OpenBSDにPHPをインストールするには、バイナリパッケージを使用することが
    推奨されておりまたもっとも簡単な方法です。
    コアパッケージは他の種々のモジュールと分けられており、別個に独立して
    インストールまたは削除することができます。OpenBSDのCDやFTPサイトから
    必要なファイルを見つけることができます。
   </simpara>
   <simpara>
    インストールに必要なメインのパッケージは<filename>php4-core-4.2.3.tgz</filename>
    です。これには基本エンジン(とgettextとiconv)が含まれています。次に、
    <filename>php4-mysql-4.2.3.tgz</filename>や
    <filename>php4-imap-4.2.3.tgz</filename>のようなモジュールパッケージを
    探してください。これらのモジュールを &php.ini;上で有効/無効にするには
    <command>phpxs</command>を使用する必要があります。
   </simpara>
   <example id="install.openbsd.ports.example">
    <title>OpenBSD パッケージインストールの例</title>
    <programlisting>
<![CDATA[
# pkg_add php4-core-4.2.3.tgz
# /usr/local/sbin/phpxs -s
# cp /usr/local/share/doc/php4/php.ini-recommended /var/www/conf/php.ini
  (add in mysql)
# pkg_add php4-mysql-4.2.3.tgz
# /usr/local/sbin/phpxs -a mysql
  (add in imap)
# pkg_add php4-imap-4.2.3.tgz
# /usr/local/sbin/phpxs -a imap
  (remove mysql as a test)
# pkg_delete php4-mysql-4.2.3
# /usr/local/sbin/phpxs -r mysql
  (install the PEAR libraries)
# pkg_add php4-pear-4.2.3.tgz
]]>
    </programlisting>
   </example>
   <simpara>
   OpenBSDのバイナリパッケージに関する詳細はマニュアルの
   <ulink url="&url.openbsd.packages;">packages(7)</ulink>を
   ご覧ください。
   </simpara>
 </sect2>
 <sect2 id="install.openbsd.ports">
  <title>Using Ports</title>
  <simpara>
   <ulink url="&url.openbsd.ports;">portsツリー</ulink>を使ってPHPのソースを
   コンパイルすることもできます。ただしこの方法はOpenBSDに詳しいユーザにのみ
   推奨されます。PHP4ポートはcore, extensions, peraの3つのサブディレクトリに
   分割されています。extensionsディレクトリはサポートされている全てのPHP
   モジュールのサブパッケージを生成します。
   これらのうちのいくつかのモジュールを生成したくない場合には、
   <command>no_*</command> FLAVOR を使用してください。例えば、imapモジュール
   のビルドをスキップするにはFLAVOR を <command>no_imap</command>に
   セットします。
  </simpara>
 </sect2>
 <sect2 id="install.openbsd.older">
  <title>Older Releases</title>
  <simpara>
   Older releases of OpenBSD used the FLAVORS system to compile up
   a statically linked PHP.  Since it is hard to generate binary packages using
   this method, it is now deprecated.  You can still use the old stable
   ports trees if you wish, but they are unsupported by the OpenBSD team.
   If you have any comments about this, the current maintainer for the port
   is <ulink url='mailto:avsm@openbsd.org'>Anil Madhavapeddy</ulink>.
  </simpara>
 </sect2>
</sect1>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
