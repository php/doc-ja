<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision: 1.3 $ -->
<reference id="ref.shmop">
  <title>共有メモリ関数(shmop)</title>
  <titleabbrev>shmop</titleabbrev>
  
  <partintro>
   <section id="shmop.intro">
    &reftitle.intro;
    <para>
     shmop は、共有メモリセグメントをPHPから簡単に読み書きまたは作成、
     削除することを可能にする一連の関数です。Windows上では共有メモリが
     サポートされないため、これらの関数は動作しません。
     しかし、Windows 2000以降、<filename>php_shmop.dll</filename>を
     &php.ini;で有効にすることにより、この機能を有効にすることができま
     す。
    </para>
    <note>
     <simpara>
      PHP 4.0.3では、以下の関数に接頭辞<literal>shmop</literal>ではな
      く<literal>shm</literal>が付いていました。
      rather than 
     </simpara>
    </note>
   </section>
   
   <section id="shmop.requirements">
    &reftitle.required;
    &no.requirement;
   </section>

   &reference.shmop.configure;

   <section id="shmop.configuration">
    &reftitle.runtime;
    &no.config;
   </section>

   <section id="shmop.resources">
    &reftitle.resources;
    <para>
    </para>
   </section>

   <section id="shmop.constants">
    &reftitle.constants;
    &no.constants;
   </section>

   <section id="shmop.examples">
    &reftitle.examples;
    <para>
     <example>
      <title>共有メモリ操作の概要</title> 
      <programlisting role="php">
<![CDATA[
<?php
   
// システムID 0xff3を有する 100 バイトの共有メモリブロックを作成する
$shm_id = shmop_open(0xff3, "c", 0644, 100);
if(!$shm_id) {
	echo "共有メモリセグメントを作成できませんでした。\n";
}

// 共有メモリのブロック長を得る
$shm_size = shmop_size($shm_id);
echo "SHM ブロックサイズ: ".$shm_size. " が作成されました。\n";

// 共有メモリにテスト用の文字列を書き込んでみる
$shm_bytes_written = shmop_write($shm_id, "my shared memory block", 0);
if($shm_bytes_written != strlen("my shared memory block")) {
	echo "データ全体を書き込めませんでした。\n";
}

// その文字列を再び読み込んでみる
$my_string = shmop_read($shm_id, 0, $shm_size);
if(!$my_string) {
	echo "共有メモリブロックから読み込めません。\n";
}
echo "共有メモリ内のデータは次のようになります: ".$my_string."\n";

// ブロックを削除し、共有メモリセグメントを閉じる
if(!shmop_delete($shm_id)) {
	echo "共有メモリブロックに削除用のマークを付けることができません。";
}
shmop_close($shm_id);
   
?>
]]>
      </programlisting>
     </example>
    </para>
   </section>
  </partintro>  

&reference.shmop.functions;

 </reference> 

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
-->

