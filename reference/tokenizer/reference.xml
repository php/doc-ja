<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision: 1.1 $ -->
 <reference id="ref.tokenizer">
  <title>Tokenizer関数</title>
  <titleabbrev>Tokenizer関数</titleabbrev>

  <partintro>
   <section id="tokenizer.intro">
    &reftitle.intro;
    <para>
     tokenizer関数は、Zend Engineに組み込まれたPHP tokenizerへのインター
     フェイスを提供します。以下の関数により、
     字句解析レベルの言語処理を行うことなく、PHPソースを解析/修正する
     ツールを作成することが可能となります。
    </para>
    <para>
     <link linkend="tokens">トークンに関する付録</link>も参照して下さい。
    </para>
   </section>

   <section id="tokenizer.requirements">
    &reftitle.required; 
    &no.requirement; 
   </section> 

   &reference.tokenizer.configure;

   &reference.tokenizer.constants;

   <section id="tokenizer.examples">
    &reftitle.examples;
    <para>
     以下にtokenizerを用いた簡単なPHPスクリプト例を示します。この例は、
     PHPファイルを読み込み、ソースから全てのコメントを削除、コードのみ
     を出力するものです。
    </para>
    <example>
     <title>tokenizerによりコメントを削除する</title>
     <programlisting role="php">
<![CDATA[
<?php
  $source = file_get_contents("somefile.php");
  $tokens = token_get_all($source);
  foreach ($tokens as $token) {
    if (is_string($token)) {
      // 簡単な1文字毎のトークン
      echo $token;
    } else {
      // トークン配列
      list($id, $text) = $token;
      switch($id) { 
        case T_COMMENT: 
        case T_ML_COMMENT:
          // コメントの場合は何もしない
          break;
        default:
          // それ以外の場合 -> "そのまま"出力
          echo $text;
          break;
      }
    }
  }
?>
]]>
     </programlisting>
    </example>    
   </section>


  </partintro>

&reference.tokenizer.functions;

 </reference>
<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
-->

