<?xml version='1.0' encoding='utf-8'?>
<!-- $Revision: 1.4 $ -->
<!-- EN-Revision: 1.12 Maintainer: shimooka Status: ready -->
<!-- CREDITS: hirokawa -->
<refentry id="function.PDO-construct">
 <refnamediv>
  <refname>PDO::__construct</refname>
  <refpurpose>
   データベースへの接続を表す PDO インスタンスを生成する
  </refpurpose>
 </refnamediv>
 <refsect1 role="description">
  &reftitle.description;
  <methodsynopsis>
   <type>PDO</type><methodname>PDO::__construct</methodname>
   <methodparam><type>string</type><parameter>dsn</parameter></methodparam>
   <methodparam choice="opt"><type>string</type><parameter>username</parameter></methodparam>
   <methodparam choice="opt"><type>string</type><parameter>password</parameter></methodparam>
   <methodparam choice="opt"><type>array</type><parameter>driver_options</parameter></methodparam>
  </methodsynopsis>
  &warn.experimental.func;
  <para>
   指定されたデータベースへの接続を表すPDOインスタンスを生成します。
  </para>
 </refsect1>
 <refsect1 role="parameters">
  &reftitle.parameters;
  <para>
   <variablelist>
    <varlistentry><term>dsn</term>
     <listitem>
      <para>
       データソース名(Data Source Name)またはDSN。データベースに接続するために
       必要な情報が含まれます。
      </para>
      <para>
       一般に、DSNはPDOドライバ名の後にコロンが続き、
       各PDOドライバに固有の接続構文が続きます。
       各ドライバの例を以下に示します。
      </para>
      <para>
       <variablelist>
        <varlistentry>
         <term><constant>PDO_DBLIB</constant></term>
         <listitem>
          <para>
           DSN接頭時は、コンパイル時にリンクされたライブラリの種類により
           <userinput>sybase:</userinput>または
           <userinput>mssql:</userinput>となります。
          </para>
          <para><userinput>sybase:host=localhost; dbname=testdb</userinput></para>
          <para><userinput>mssql:host=localhost; dbname=testdb</userinput></para>
         </listitem>
        </varlistentry>
        <varlistentry>
         <term><constant>PDO_FIREBIRD</constant></term>
         <listitem>
          <para><userinput>firebird:User=john;Password=mypass;Database=DATABASE.GDE;DataSource=localhost;Port=3050</userinput></para>
         </listitem>
        </varlistentry>
        <varlistentry>
         <term><constant>PDO_MYSQL</constant></term>
         <listitem>
          <para><userinput>mysql:host=localhost;dbname=testdb</userinput></para>
         </listitem>
        </varlistentry>
        <varlistentry>
         <term><constant>PDO_OCI</constant></term>
         <listitem>
          <para><filename>tnsnames.ora</filename>を使用するには、以下のようにします:</para>
          <para><userinput>oci:mydb</userinput></para>
          <para>instantclientを使用するには、以下のようにします:</para>
          <para><userinput>oci:dbname=//localhost:1521/testdb</userinput></para>
         </listitem>
        </varlistentry>
        <varlistentry>
         <term><constant>PDO_ODBC</constant></term>
         <listitem>
          <para><userinput>odbc:DSN=SAMPLE;UID=john;PWD=mypass</userinput></para>
          <para>DSN=SAMPLE は ODBCドライバマネージャで設定されたSAMPLE データソースを参照します
          </para>
         </listitem>
        </varlistentry>
        <varlistentry>
         <term><constant>PDO_PGSQL</constant></term>
         <listitem>
          <para><userinput>pgsql:host=localhost port=5432 dbname=testdb user=john password=mypass</userinput></para>
          <para>
           <varname>user</varname> と <varname>password</varname> をDSNで指定した場合、
           <parameter>username</parameter>および
           <parameter>password</parameter>パラメータはオプションとなることに注意して
           ください。
           これらを指定した場合、接続文字列の最後に追加されます。
          </para>
         </listitem>
        </varlistentry>
        <varlistentry>
         <term><constant>PDO_SQLITE</constant></term>
         <listitem>
          <para><userinput>sqlite:/path/to/database</userinput></para>
          <para>データベースをメモリ上に作成する場合は、
          以下のようにしてください:</para>
          <para><userinput>sqlite::memory:</userinput></para>
         </listitem>
        </varlistentry>
        <varlistentry>
         <term><constant>sqlite (version 2)</constant></term>
         <listitem>
          <para><userinput>sqlite2:/path/to/database</userinput></para>
          <para>データベースをメモリ上に作成する場合は、
          以下のようにしてください:</para>
          <para><userinput>sqlite2::memory:</userinput></para>
          <note>
           <para>
            SQLite 2 PDO ドライバは、以前のバージョンの PHP
            で作成した古い SQLite 2 データベースへのアクセス手段を提供します。
            この場合、より優れた機能やパフォーマンスのため、SQLite 3
            (PDO_SQLITE ドライバ) へマイグレーションする事が推奨されます。
           </para>
          </note>
         </listitem>
        </varlistentry>
       </variablelist>
      </para>
      <para>
       <parameter>dsn</parameter> パラメータは、データベースへの接続を生成する
       ために必要な引数を指定する方法として、3種類の方法をサポートします。
      </para>
      <para>
       <variablelist>
        <varlistentry><term>ドライバ呼び出し</term>
         <listitem>
          <para>
           <parameter>dsn</parameter> contains the full DSN.
          </para>
         </listitem>
        </varlistentry>
        <varlistentry><term>URI呼び出し</term>
         <listitem>
          <para>
           <parameter>dsn</parameter> は、<userinput>uri:</userinput>の後に
           DSN文字列を含むファイルの位置を定義するURIが続く形式となります。
           このURIには、ローカルファイルまたはリモートURLを指定することができます。
          </para>
          <para><userinput>uri:file:///path/to/dsnfile</userinput></para>
         </listitem>
        </varlistentry>
        <varlistentry><term>エイリアス</term>
         <listitem>
          <para>
           <parameter>dsn</parameter> は、DSN文字列を定義する
           &php.ini;の<literal>pdo.dsn.<parameter>name</parameter></literal>へ
           マップする名前<parameter>name</parameter> からなります。
          </para>
          <note>
           <para>
            エイリアスは、&htaccess; や &httpd.conf;ではなく、
            &php.ini;で定義する必要があります。
           </para>
          </note>
         </listitem>
        </varlistentry>
       </variablelist>
      </para>
     </listitem>
    </varlistentry>
    <varlistentry><term>username</term>
     <listitem>
      <para>
       DSN文字列のユーザ名。このパラメータは、いくつかのPDOドライバではオプションです。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry><term>password</term>
     <listitem>
      <para>
       パスワード。DSN文字列で必要とされる場合に指定。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry><term>driver_options</term>
     <listitem>
      <para>
       ドライバ固有の接続オプションを指定するキー=&gt; 値の配列。
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </para>
 </refsect1>

 <refsect1 role="returnvalues">
  &reftitle.returnvalues;
  <para>
   成功時にPDOオブジェクトを返します。
  </para>
 </refsect1>

 <refsect1 role="exceptions">
  &reftitle.exceptions;
  <para>
   <function>PDO::construct</function> は、
   指定されたデータベースへの接続に失敗した場合、
   PDOException を投げます。
  </para>
 </refsect1>

 <refsect1 role="examples">
  &reftitle.examples;
  <para>
   <example><title>ドライバ呼び出しによりPDOインスタンスを生成する</title>
    <programlisting role="php">
<![CDATA[
<?php
// Connect to an ODBC database using driver invocation
$dsn = 'mysql:dbname=testdb;host=127.0.0.1';
$user = 'dbuser';
$password = 'dbpass';

try {
    $dbh = new PDO($dsn, $user, $password);
} catch (PDOException $e) {
    echo 'Connection failed: ' . $e->getMessage();
}

?>
]]>
    </programlisting>
   </example>
   <example><title>URI呼び出しによりPDOインスタンスを生成する</title>
    <para>
     以下の例では、ファイル <filename>/usr/local/dbconnect</filename> が存在し、
     PHPがこのファイルを読み込む権限を有していることを仮定します。
     このファイルには、 PDO_ODBCによりDB2データベースに接続するためのPDO DSNが
     含まれています。
    </para>
    <screen>
<![CDATA[
odbc:DSN=SAMPLE;UID=john;PWD=mypass
]]>
    </screen>
    <para>
     これにより、PHPスクリプトでは、単に<literal>uri:</literal> パラメータを渡し、
     ファイルのURIを示すだけでデータベース接続を作成することができるようになります。
    </para>
    <programlisting role="php">
<![CDATA[
<?php
// Connect to an ODBC database using driver invocation
$dsn = 'uri:file:///usr/local/dbconnect';
$user = '';
$password = '';

try {
    $dbh = new PDO($dsn, $user, $password);
} catch (PDOException $e) {
    echo 'Connection failed: ' . $e->getMessage();
}

?>
]]>
    </programlisting>
   </example>
   <example><title>エイリアスによりPDOインスタンスを生成する</title>
    <para>
     以下の例では、&php.ini; にエイリアス<literal>mydb</literal>のみで
     MySQLデータベースに接続できるようにするための以下のエントリが含まれることを
     仮定します。
     <programlisting role="ini">
[PDO]
pdo.dsn.mydb="mysql:dbname=testdb;host=localhost"
     </programlisting>
    </para>
    <programlisting role="php">
<![CDATA[
<?php
// Connect to an ODBC database using an alias
$dsn = 'mydb';
$user = '';
$password = '';

try {
    $dbh = new PDO($dsn, $user, $password);
}catch (PDOException $e) {
    echo 'Connection failed: ' . $e->getMessage();
}

?>
]]>
    </programlisting>
   </example>
  </para>
 </refsect1>
</refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
