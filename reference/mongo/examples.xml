<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision: 1.2 $ -->
<!-- EN-Revision: 1.4 Maintainer: takagi Status: ready -->

<chapter xml:id="mongo.examples" xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
 &reftitle.examples;
 <para>
  この例では、データベースへの接続から
  オブジェクトの挿入、オブジェクトへの問い合わせ、
  問い合わせ結果の処理、そして Mongo データベースとの接続の切断までを扱います。
 </para>
 <example>
  <title>Mongo の例</title>
  <programlisting role="php">
<![CDATA[
<?php

include "mongo.php";

// 接続
$m = new Mongo();

// データベースの選択
$db = $m->selectDatabase("comedy");
$collection = $db->selectCollection("cartoons");

// 要素の追加
$obj = array( "title" => "Calvin and Hobbes", "author" => "Bill Watterson" );
$collection->insert($obj);

// 別の "シェイプ" による別の要素の追加
$obj = array( "title" => "XKCD", "online" => true );
$collection->insert($obj);

// コレクション内の全要素の検索
$cursor = $collection->find();

// 結果の反復処理
foreach ($cursor as $obj) {
    echo $obj["title"] . "\n";
}

// 接続の切断
$m->close();

?>
]]>
  </programlisting>
  &example.outputs.similar;
  <screen>
<![CDATA[
Calvin and Hobbes
XKCD
]]>
  </screen>
 </example>
</chapter>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->

