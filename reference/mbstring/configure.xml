<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision$ -->
<!-- EN-Revision: 96c9d88bad9a7d7d44bfb7f26c226df7ee9ddf26 Maintainer: hirokawa Status: ready -->
<!-- CREDITS: takagi -->
<section xml:id="mbstring.installation" xmlns="http://docbook.org/ns/docbook">
  &reftitle.install;
  <para>
   <literal>mbstring</literal> は拡張モジュールです。つまり、デフォルトでは
   有効にならないということです。
   <literal>configure</literal> スクリプトでモジュールを有効にする必要が
   あります。詳細は、<link linkend="install">インストール</link>の
   節を参照してください。
  </para>
  <simpara>
   <literal>mbstring</literal> モジュールに関係する設定オプションは
   以下のとおりです。
  </simpara>
  <para>
  <itemizedlist>
   <listitem>
    <para>
     <option role="configure">--enable-mbstring</option>:
     <literal>mbstring</literal> 関数を有効にします。このオプションは、
     <literal>mbstring</literal> 関数を利用するために必要です。
    </para>
    <para>
     <literal>mbstring</literal> 拡張モジュールを使用するためには、
     <productname>libmbfl</productname>ライブラリが必要です。
     <productname>libmbfl</productname>は、<literal>mbstring</literal> 
     拡張モジュールにバンドルされています。システムにインストールされている
     <productname>libmbfl</productname> を利用する場合、
     <option role="configure">--with-libmbfl[=DIR]</option> を指定します。
    </para>
    <para>
     PHP 4.3.0 以降、<literal>mbstring</literal> 拡張モジュールは
     日本語のほかに中国語 (簡体字)・中国語 (繁体字)・
     韓国語・ロシア語をサポートするように機能拡張されました。
    </para>
    <para>
     PHP 4.3.4以前のバージョンの場合、
     この機能を使用するには、<option role="configure">--enable-mbstring=LANG</option>の
     <literal>LANG</literal> パラメータに
     以下のオプションのいずれかを追加する必要があります。
     <option role="configure">--enable-mbstring=cn</option> を
     使用した場合、簡体字中国語のエンコーディングがサポートされます。
     <option role="configure">--enable-mbstring=tw</option> を
     使用した場合、繁体字中国語のエンコーディングがサポートされます。
     <option role="configure">--enable-mbstring=kr</option> を
     使用した場合、韓国語のエンコーディングがサポートされます。
     <option role="configure">--enable-mbstring=ru</option> を
     使用した場合、ロシア語のエンコーディングがサポートされます。そして、
     <option role="configure">--enable-mbstring=ja</option> (デフォルト)を
     使用した場合、日本語のエンコーディングがサポートされます。
     <option role="configure">--enable-mbstring=all</option>
     を指定した場合、サポートされるすべての文字エンコーディングが有効となります。
     以前のバージョンとの互換性のため、何もオプションを指定せずに
     <option role="configure">--enable-mbstring</option> を使用した場合は
     日本語のエンコーディングがサポートされます。
    </para>
    <note>
     <para>
      PHP 4.3.4以降では、<option role="configure">--enable-mbstring</option>により、
      <productname>libmbfl</productname>でサポートされている全てのエンコーディングが有効となるよう
      になっています。
     </para>
    </note>
   </listitem>
    <listitem>
     <para>
      <option role="configure">--enable-mbstr-enc-trans</option> :
      <literal>mbstring</literal> 変換エンジンを使用した、HTTP 入力の
      文字エンコーディング変換を有効にします。この機能が有効の場合、
      HTTP 入力文字エンコーディングは、自動的に
      <literal>mbstring.internal_encoding</literal> に変換されます。
     </para>
     <note>
      <para>
       PHP 4.3.0 以降、このオプション
       <option role="configure">--enable-mbstr-enc-trans</option>
       は廃止され、実行時の設定 
       <literal>mbstring.encoding_translation</literal>
       に変更となります。HTTP 入力文字エンコーディング変換は、
       このオプションを <literal>On</literal> に設定した場合のみ
       有効となります
       (デフォルトは <literal>Off</literal>です)。
      </para>
     </note>
    </listitem>
    <listitem>
     <para>
      <option role="configure">--disable-mbregex</option> :
      マルチバイト対応の正規表現関数を無効にします。
     </para>
    </listitem>
   </itemizedlist>
  </para>
</section>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"~/.phpdoc/manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
