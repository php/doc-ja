<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision: 1.4 $ -->
<!-- splitted from ./ja/functions/misc.xml, last change in rev 1.1 -->
<!-- EN-Revision: 1.8 Maintainer: hirokawa Status: ready -->
<!-- CREDITS: takagi -->
  <refentry id="function.uniqid">
   <refnamediv>
    <refname>uniqid</refname>
    <refpurpose>ユニークな ID を生成する</refpurpose>
   </refnamediv>
   <refsect1>
    <title>説明</title>
     <methodsynopsis>
      <type>string</type><methodname>uniqid</methodname>
      <methodparam choice="opt"><type>string</type><parameter>prefix</parameter></methodparam>
      <methodparam choice="opt"><type>bool</type><parameter>more_entropy</parameter></methodparam>
     </methodsynopsis>
    <simpara>
     <function>uniqid</function> は、ミリ秒単位の現在時刻に基づき
     先頭辞（prefix）を付けたユニークな ID を返します。prefix は
     オプションですが、有用です。例えば、いくつかのホスト上で同時に
     ID を生成させる場合、全く同じ ID が生成されるのを防ぐことができます。
     PHP 4.3.1 以前では、<parameter>prefix</parameter> には 114 文字までの
     長さしか指定できませんでした。
    </simpara>
    <simpara>
     オプションの <parameter>more_entropy</parameter> パラメータが &true; の場合、
     <function>uniqid</function> は、より結果をユニークにするために、
     combined linear congruential generator を用いた結果を返り値の最後に追加します。
    </simpara>
    <simpara>
     <parameter>prefix</parameter> を空とした場合、返される文字列の長さは、
     13 文字となります。<parameter>more_entropy</parameter> が &true; の場合は、
     23 文字となります。
    </simpara>
    <note>
     <simpara>
      <parameter>prefix</parameter> パラメータは PHP 5 ではオプションとなります。
     </simpara>
    </note>
    <para>
     ユニークな ID またはトークンが必要な場合、そして、ネットワーク経由で
     ユーザにそのトークンを渡そうとする場合（例えば、セッションクッキー）、
     次の例のようにするのが推奨されます。
    </para>
    <para>
     <informalexample>
      <programlisting role="php">
<![CDATA[
<?php
// prefix なし
$token = md5(uniqid());

// よりよい方法。推測されにくい
$better_token = md5(uniqid(rand(), true));
?>
]]>
      </programlisting>
     </informalexample>
    </para>
    <simpara>
     この例は、極めて予測困難な 32 文字の ID（128 ビット 16 進数）
     を作成します。
    </simpara>
   </refsect1>
  </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
