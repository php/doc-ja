<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision$ -->
<!-- EN-Revision: dc1dcfb27b0a3dcbaa771035ce891c3ce31a03c7 Maintainer: takagi Status: ready -->

<refentry xml:id="amqpqueue.get" xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
 <refnamediv>
  <refname>AMQPQueue::get</refname>
  <refpurpose>次のメッセージをキューから取得する</refpurpose>
 </refnamediv>

 <refsect1 role="description">
  &reftitle.description;
  <methodsynopsis>
   <modifier>public</modifier> <type>mixed</type><methodname>AMQPQueue::get</methodname>
      <methodparam choice="opt"><type>int</type><parameter>flags</parameter></methodparam>
     </methodsynopsis>
     <para>
       次のメッセージをキューから取得します。もしメッセージがキューになければ、メッセージを取得せずすぐに &false; を返します。
       これは、<methodname>AMQPQueue::consume</methodname> メソッドの非ブロック版です。
     </para>
     <para>
      今のところ、<parameter>flags</parameter> パラメータに渡すフラグとしてサポートしているのは <constant>AMQP_AUTOACK</constant> だけです。
      このフラグを渡した場合、返されたメッセージのフレームをクライアントに送信すると、自動的にブローカによる受け付けマークがつけられます。
     </para>
    </refsect1>

    <refsect1 role="parameters">
     &reftitle.parameters;
     <para>
      <variablelist>
      <varlistentry>
       <term><parameter>flags</parameter></term>
       <listitem>
        <para>
         メソッドコールの際にサポートしているフラグのビットマスク。
         今のところ、サポートしているフラグは <constant>AMQP_AUTOACK</constant> だけです。
         この値を省略すると、<literal>amqp.auto_ack</literal> の値を使います。
        </para>
       </listitem>
      </varlistentry>
     </variablelist>
     </para>
    </refsect1>

    <refsect1 role="returnvalues">
     &reftitle.returnvalues;
     <para>
      キューから取得したメッセージを表す <classname>AMQPEnvelope</classname>
      のインスタンス、あるいは &false; を返します。
     </para>
    </refsect1>

    <refsect1 role="examples">
     &reftitle.examples;
     <para>
      <example>
       <title><methodname>AMQPQueue::get</methodname> の例</title>
       <programlisting role="php">
   <![CDATA[
   <?php

   /* デフォルトの設定で接続を作成します */
   $connection = new AMQPConnection();
   $connection->connect();

   $channel = new AMQPChannel($connection);

   /* キューオブジェクトを作成します */
   $queue = new AMQPQueue($channel);

   // キューを宣言します
   $queue->declare('myqueue');

   // メッセージを取得します
   $messages = $queue->get(AMQP_AUTOACK);

   echo $message->getBody();

   ?>
   ]]>
       </programlisting>
      </example>
     </para>
    </refsect1>

   </refentry>

   <!-- Keep this comment at the end of the file
   Local variables:
   mode: sgml
   sgml-omittag:t
   sgml-shorttag:t
   sgml-minimize-attributes:nil
   sgml-always-quote-attributes:t
   sgml-indent-step:1
   sgml-indent-data:t
   indent-tabs-mode:nil
   sgml-parent-document:nil
   sgml-default-dtd-file:"~/.phpdoc/manual.ced"
   sgml-exposed-tags:nil
   sgml-local-catalogs:nil
   sgml-local-ecat-files:nil
   End:
   vim600: syn=xml fen fdm=syntax fdl=2 si
   vim: et tw=78 syn=sgml
   vi: ts=1 sw=1
   -->
