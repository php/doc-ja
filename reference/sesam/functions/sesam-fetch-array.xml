<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision: 1.8 $ -->
<!-- EN-Revision: 1.10 Maintainer: hirokawa Status: ready -->
<refentry xml:id="function.sesam-fetch-array" xmlns="http://docbook.org/ns/docbook">
 <refnamediv>
  <refname>sesam_fetch_array</refname>
  <refpurpose>連想配列としてレコードを 1 件取得する</refpurpose>
 </refnamediv>

 <refsect1 role="description">
  &reftitle.description;
  <methodsynopsis>
   <type>array</type><methodname>sesam_fetch_array</methodname>
   <methodparam><type>string</type><parameter>result_id</parameter></methodparam>
   <methodparam choice="opt"><type>int</type><parameter>whence</parameter></methodparam>
   <methodparam choice="opt"><type>int</type><parameter>offset</parameter></methodparam>
  </methodsynopsis>
  <para>
   <function>sesam_fetch_array</function> は、
   <function>sesam_fetch_row</function> の連想配列版です。データを
   結果配列の数値添字に保存する代わりに、データをフィールド名を
   キーとして連想配列に保存します。
  </para>
  <para>
   <function>sesam_fetch_array</function> は、
   指定した結果 ID が指す結果から 1 件分のレコードを取得します。
   レコードは、連想配列として返されます。各結果カラムは、そのカラム
   (またはフィールド) 名に等しい連想配列の要素に保存されます。カラム名は、
   小文字に変換されます。
  </para>
  <para>
   フィールド名がないカラム (例えば、数値演算の結果) および空の
   フィールドは、配列に保存されません。また、同じカラム名にが
   2 つ以上ある場合、最後のカラムが優先されます。この場合、
   <function>sesam_fetch_row</function> をコールするかそのカラムへの
   エイリアスを作成してください。
   <informalexample>
    <programlisting role="sesam">
<![CDATA[
SELECT TBL1.COL AS FOO, TBL2.COL AS BAR FROM TBL1, TBL2
]]>
    </programlisting>
   </informalexample>
  </para>
  <para>
   特別な処理により "複数フィールド" カラム (もしくは同じカラム名を
   有するカラム) を取得することが可能になります。"複数フィールド" の
   各カラムに関して、添字名は、文字列 "(n)" を付加することにより
   構築されます。ただし、n は複数フィールドのカラムの副添字であり、
   1 から宣言済みの反復数までの範囲となります。クエリ構文で使用される
   表記に一致させるために添字はゼロを基準にしていません。
   次のように宣言されているあるカラムの場合、
   <informalexample>
    <programlisting role="sesam">
<![CDATA[
CREATE TABLE ... ( ... MULTI(3) INT )
]]>
    </programlisting>
   </informalexample>
   個々の "複数フィールド" カラムに関して連想添字は、それぞれ
   <literal>"multi(1)"</literal>,<literal>"multi(2)"</literal>, 
   <literal>"multi(3)"</literal> になります。
  </para>
  <para>
   <function>sesam_fetch_array</function> を続けてコールした場合、
   結果集合の次の (スクロール属性に応じて前または n 番目の前/後)
   レコードまたはレコードがもうない場合に &false; を返します。
  </para>
 </refsect1>

 <refsect1 role="parameters">
  &reftitle.parameters;
  <para>
   <variablelist>
    <varlistentry>
     <term><parameter>result_id</parameter></term>
     <listitem>
      <para>
       <function>sesam_query</function> が返す結果 ID。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><parameter>whence</parameter></term>
     <listitem>
      <para>
       <parameter>whence</parameter> は "スクロール型" カーソルで
       取得処理を行うためのオプションパラメータで、次のような定義済みの
       定数を設定することが可能です。
       <table>
        <title><parameter>"whence"</parameter> パラメータで有効な値</title>
        <tgroup cols="3">
         <thead>
          <row>
           <entry>値</entry>
           <entry>定数</entry>
           <entry>意味</entry>
          </row>
         </thead>
         <tbody>
          <row>
           <entry>0</entry>
           <entry><constant>SESAM_SEEK_NEXT</constant></entry>
           <entry>
            連続的に読み込む (取得後、内部デフォルト値は
            <literal>SESAM_SEEK_NEXT</literal> に設定されます)。
           </entry>
          </row>
          <row>
           <entry>1</entry>
           <entry><constant>SESAM_SEEK_PRIOR</constant></entry>
           <entry>
            連続的に後向きに読み込む (取得後、内部デフォルト値は
            <literal>SESAM_SEEK_PRIOR</literal> に設定されます)。
           </entry>
          </row>
          <row>
           <entry>2</entry>
           <entry><constant>SESAM_SEEK_FIRST</constant></entry>
           <entry>
            最初のレコードに移動する (取得後、デフォルト値は
            <literal>SESAM_SEEK_NEXT</literal> に設定されます)。
           </entry>
          </row>
          <row>
           <entry>3</entry>
           <entry><constant>SESAM_SEEK_LAST</constant></entry>
           <entry>
            最後のレコードに移動する (取得後、デフォルト値は
            <literal>SESAM_SEEK_PRIOR</literal> に設定されます)。
           </entry>
          </row>
          <row>
           <entry>4</entry>
           <entry><constant>SESAM_SEEK_ABSOLUTE</constant></entry>
           <entry>
            <parameter>offset</parameter> (0 が先頭。取得後、内部
            デフォルト値は <literal>SESAM_SEEK_ABSOLUTE</literal>
            に設定されます。内部のオフセット値は、自動的に増加します)
            で指定した絶対レコード番号に移動します。
           </entry>
          </row>
          <row>
           <entry>5</entry>
           <entry><constant>SESAM_SEEK_RELATIVE</constant></entry>
           <entry>
            カレントのスクロール位置に対して相対位置に移動します。ただし、
            <parameter>offset</parameter> は正または負の値を指定可能です。
           </entry>
          </row>
         </tbody>
        </tgroup>
       </table>
       このパラメータは、"スクロール型" カーソルでのみ有効です。
      </para>
      <para>
       "スクロール型" カーソルを使用している場合、カーソルは結果集合を
       自由に移動可能です。<parameter>whence</parameter> パラメータが
       省略された場合、スクロールの型には、グローバルなデフォルト値
       (<literal>SESAM_SEEK_NEXT</literal> に初期化されており、
       <function>sesam_seek_row</function> で設定可能です) が使用されます。
       <parameter>whence</parameter> が指定された場合、その値は
       グローバルデフォルト値に置換されます。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><parameter>offset</parameter></term>
     <listitem>
      <para>
       <parameter>offset</parameter> は、オプションのパラメータであり、
       <parameter>whence</parameter> が
       <literal>SESAM_SEEK_RELATIVE</literal> または
       <literal>SESAM_SEEK_ABSOLUTE</literal> のどちらかである場合にのみ
       評価されます (そして必要とされます)。このパラメータは、
       "スクロール型" カーソルでのみ有効です。
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </para>
 </refsect1>

 <refsect1 role="returnvalues">
  &reftitle.returnvalues;
  <para>
   取得したレコードに対応する配列またはレコードがもうない場合には
   &false; を返します。
  </para>
 </refsect1>

 <refsect1 role="examples">
  &reftitle.examples;
  <para>
   <example>
    <title>SESAM 配列の取得</title>
    <programlisting role="php">
<![CDATA[
<?php
$result = sesam_query("SELECT * FROM phone\n" .
                       "  WHERE LASTNAME='" . strtoupper($name) . "'\n".
                       "  ORDER BY FIRSTNAME", 1);
if (!$result) {
    /* ... エラー ... */
}
// テーブルの表示
echo "<table border=\"1\">\n";
while (($row = sesam_fetch_array($result)) && count($row) > 0) {
    echo "<tr>\n";
    echo "<td>" . htmlspecialchars($row["firstname"]) . "</td>\n";
    echo "<td>" . htmlspecialchars($row["lastname"]) . "</td>\n";
    echo "<td>" . htmlspecialchars($row["phoneno"]) . "</td>\n";
    echo "</tr>\n";
}
echo "</table>\n";
sesam_free_result($result);
?>
]]>
    </programlisting>
   </example>
  </para>
 </refsect1>

 <refsect1 role="seealso">
  &reftitle.seealso;
  <para>
   <simplelist>
    <member><function>sesam_fetch_row</function></member>
   </simplelist>
  </para>
 </refsect1>

</refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
