<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision: 1.5 $ -->
<!-- splitted from ./ja/functions/imap.xml, last change in rev 1.1 -->
<!-- EN-Revision: 1.7 Maintainer: hirokawa Status: ready -->
  <refentry id="function.imap-getmailboxes">
   <refnamediv>
    <refname>imap_getmailboxes</refname>
    <refpurpose>
     メールボックスのリストを読み込み、各ボックスに関する詳細な情報を返す
    </refpurpose>
   </refnamediv>
   <refsect1>
    <title>説明</title>
     <methodsynopsis>
      <type>array</type><methodname>imap_getmailboxes</methodname>
      <methodparam><type>resource</type><parameter>imap_stream</parameter></methodparam>
      <methodparam><type>string</type><parameter>ref</parameter></methodparam>
      <methodparam><type>string</type><parameter>pattern</parameter></methodparam>
     </methodsynopsis>
    <para>
     メールボックス情報を有するオブジェクトの配列を返します。各オブジェクトには、
     メールボックスの完全な名前である <parameter>name</parameter>、
     このメールボックスの階層の区切りを示す属性 <parameter>delimiter</parameter>、
     そして <parameter>attributes</parameter> が存在します。
     <parameter>attributes</parameter> はビットマスクであり、
     次のものについて調べることができます。
     <itemizedlist>
      <listitem>
       <simpara>
        <constant>LATT_NOINFERIORS</constant> - このメールボックスには
        「子供」はいません（このボックスの中にメールボックスはありません）。
       </simpara>
      </listitem>
     <listitem>
       <simpara>
        <constant>LATT_NOSELECT</constant> - 単なるコンテナであり、
        メールボックスではありません - これをオープンすることはできません。
       </simpara>
      </listitem>
      <listitem>
       <simpara>
        <constant>LATT_MARKED</constant> - このメールボックスにはマークが
        つけられています。UW-IMAPD でのみ使用されます。
       </simpara>
      </listitem> 
      <listitem>
       <simpara> 
        <constant>LATT_UNMARKED</constant> - このメールボックスにはマークが
        つけられていません。UW-IMAPD でのみ使用されます。
       </simpara>
      </listitem> 
     </itemizedlist>
    </para>
    <para>
     非 ASCII 文字を含むメールボックス名は、
     <function>imap_utf7_decode</function> を使用してエンコード
     ／デコードが行われます。
    </para>
    <para>
     通常、<parameter>ref</parameter> は
     <function>imap_open</function> で述べられているサーバ定義です。
     また <parameter>pattern</parameter> では検索を開始するメールボックスの
     階層を指定します。全メールボックスを検索したい場合には
     <parameter>pattern</parameter> に '*' を渡してください。
    </para>
    <para>
     <parameter>pattern</parameter> の中で使用できる特別な文字として
     '*' および '%' があります。'*' は、全てのメールボックスを意味します。
     <parameter>pattern</parameter> に '*' を指定した場合、メールボックス
     階層全体のリストが得られます。'%' は現在のレベルのみを意味します。
     '%' を <parameter>pattern</parameter> に指定した場合、
     トップレベルのメールボックスのみが返されます。UW_IMAPD を使用した場合、
     '~/mail/%' は ~/mail ディレクトリの全てのメールボックスを返しますが、
     そのディレクトリのサブフォルダーにあるメールボックスは返しません。
    </para>
    <para>
     <example>
      <title><function>imap_getmailboxes</function> の例</title>
      <programlisting role="php">
<![CDATA[
<?php
$mbox = imap_open("{your.imap.host}", "username", "password", OP_HALFOPEN)
      or die("接続できません: " . imap_last_error());
 
$list = imap_getmailboxes($mbox, "{your.imap.host}", "*");
if (is_array($list)) {
    foreach ($list as $key => $val) {
        echo "($key) ";
        echo imap_utf7_decode($val->name) . ",";
        echo "'" . $val->delimiter . "',";
        echo $val->attributes . "<br />\n";
    }
} else {
    echo "imap_getmailboxes failed: " . imap_last_error() . "\n";
}
 
imap_close($mbox);
?>
]]>
      </programlisting>
     </example>
    </para>
    <para>
     <function>imap_getsubscribed</function> も参照ください。
    </para>
   </refsect1>
  </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
