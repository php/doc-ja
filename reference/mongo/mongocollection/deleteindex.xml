<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision$ -->
<!-- EN-Revision: d1befb8a78bf4ae181c13a5d5a9ae4f03bb5547c Maintainer: takagi Status: ready -->

<refentry xml:id="mongocollection.deleteindex" xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
 <refnamediv>
  <refname>MongoCollection::deleteIndex</refname>
  <refpurpose>コレクションからインデックスを削除する</refpurpose>
 </refnamediv>

 <refsect1 role="description">
  &reftitle.description;
  <methodsynopsis>
   <modifier>public</modifier> <type>array</type><methodname>MongoCollection::deleteIndex</methodname>
   <methodparam><type>string|array</type><parameter>keys</parameter></methodparam>
  </methodsynopsis>
  <para>
   このメソッドは、以下と同等です。
  </para>
  <programlisting role="php">
<![CDATA[
<?php

public function deleteIndexes($keys) {
  // toIndexString は protected メソッドで、文字列や配列そしてオブジェクトを
  // インデックス名に変換します
  $index = $this->toIndexString($keys);

  return $this->db->command(array("deleteIndexes" => $this->getName(), 
    "index" => $index);
}

?>
]]>
  </programlisting>
  <para>
   インデックスの作成時に、一意な名前が与えられます。この名前は、ユーザが設定する
   (<function>MongoCollection::ensureIndex</function> の "name" オプション)
   こともあれば、キーの名前や方向の組み合わせからドライバが自動生成することもあります。
   この名前を使って、<function>MongoCollection::deleteIndex</function>
   による削除を行います。
  </para>
  <para>
   残念ながら、<function>MongoCollection::ensureIndex</function>
   が生成する名前はシェルで作るものとは微妙に異なります。
   そして過去との互換性の問題のため、<function>MongoCollection::deleteIndex</function>
   では自分で設定した名前のインデックスを削除できません。
   シェルで作ったり自分で名前を設定したりしたインデックスを削除するいちばんの方法は、
   データベースコマンド <literal>deleteIndexes</literal>
   を使うことになります。
  </para>
  <para>
   "superfast query" という名前のインデックスを作った場合、それを削除するには次のようにします。
  </para>
  <programlisting role="php">
<![CDATA[
<?php

$db->command(array("deleteIndexes" => $collection->getName(), "index" => "superfast query");

?>
]]>
  </programlisting>
  <para>
   インデックスの名前を調べるには、データベースの
   <literal>system.indexes</literal> コレクションを検索して
   <literal>name</literal> フィールドを調べます。
  </para>
 </refsect1>

 <refsect1 role="parameters">
  &reftitle.parameters;
  <para>
   <variablelist>
    <varlistentry>
     <term>
      <parameter>keys</parameter>
     </term>
     <listitem>
      <para>
       インデックスを削除したいフィールド。
      </para>
     </listitem>
    </varlistentry>   
   </variablelist>
  </para>
 </refsect1>

 <refsect1 role="returnvalues">
  &reftitle.returnvalues;
  <para>
   データベースの応答を返します。
  </para>
 </refsect1>

 <refsect1 role="examples">
  &reftitle.examples;
  <example>
   <title><function>MongoCollection::deleteIndex</function> の例</title>
   <para>
    この例は、文字列パラメータおよび配列パラメータを関数に渡します。
   </para>
   <programlisting role="php">
<![CDATA[
<?php
$m = new Mongo();
$c = $m->example->indices;

// インデックスを作成します
$c->ensureIndex(array("i"=>1));

// シンプルなインデックスを削除します
$c->deleteIndex("i");


// 複数キーのインデックスを作成します
$c->ensureIndex(array("j" => 1, "k" => 1));

// 複数キーのインデックスを削除します
$c->deleteIndex(array("j" => 1, "k" => 1));
?>
]]>
   </programlisting>
  </example>
 </refsect1>
</refentry>
<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
sgml-parent-document:nil
sgml-default-dtd-file:"~/.phpdoc/manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
--> 
